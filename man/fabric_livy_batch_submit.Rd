% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fabric_livy_batch.R
\name{fabric_livy_batch_submit}
\alias{fabric_livy_batch_submit}
\title{Submit a Microsoft Fabric Livy batch job}
\usage{
fabric_livy_batch_submit(
  livy_url,
  file,
  name = NULL,
  class_name = NULL,
  args = NULL,
  jars = NULL,
  files = NULL,
  py_files = NULL,
  archives = NULL,
  conf = NULL,
  environment_id = NULL,
  target_lakehouse_id = NULL,
  tags = NULL,
  driver_memory = NULL,
  driver_cores = NULL,
  executor_memory = NULL,
  executor_cores = NULL,
  num_executors = NULL,
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  auth_args = list(),
  audience = NULL,
  verbose = TRUE,
  wait = FALSE,
  timeout = 1200,
  poll_interval = 5,
  cancel_on_timeout = TRUE
)
}
\arguments{
\item{livy_url}{A copied Livy connection URL, Livy API base URL, or enriched
Lakehouse object. Copy the batch-job URL from \strong{Lakehouse settings > Livy
endpoint}, or use an item from \code{\link[=fabric_lakehouses]{fabric_lakehouses()}}}

\item{file}{Absolute ABFS/ABFSS URI of the main Python, R, or Java/Scala
application file. It must contain a filesystem/container, host, and
non-root path, without a password, port, query, fragment, backslash, or dot
path segment. After uploading a script under a Lakehouse's \verb{Files/} area,
its \strong{Properties} dialog can copy this path. Spaces in path segments must
be percent-encoded as \verb{\%20}; raw spaces and authority whitespace are invalid}

\item{name}{Optional readable job name shown in Fabric monitoring}

\item{class_name}{Main class for a Java/Scala application; leave \code{NULL} for
Python or R scripts}

\item{args}{Optional character vector of command-line arguments passed to the
application}

\item{jars}{Optional JAR dependency URIs}

\item{files}{Optional supporting-file URIs copied to the job}

\item{py_files}{Optional Python dependency URIs, such as \code{.py} or \code{.zip}
files}

\item{archives}{Optional archive URIs that Spark should unpack}

\item{conf}{Optional named list of Spark settings or application-specific
values}

\item{environment_id}{Optional GUID of a published Fabric Environment whose
libraries and Spark settings should be used}

\item{target_lakehouse_id}{Optional Lakehouse GUID made available as
\code{spark.targetLakehouse}. Use this when the application needs an explicit
default Lakehouse context}

\item{tags}{Optional named list of string labels for monitoring}

\item{driver_memory, executor_memory}{Optional Spark memory values such as
\code{"4g"}. Leave \code{NULL} to use Fabric defaults}

\item{driver_cores, executor_cores, num_executors}{Optional Spark resource
counts. Larger values consume more capacity; leave \code{NULL} unless the
workload has been sized deliberately}

\item{tenant_id}{Microsoft Entra tenant ID. Defaults to
\code{FABRICQUERYR_TENANT_ID}}

\item{client_id}{Microsoft Entra application/client ID. Defaults to
\code{FABRICQUERYR_CLIENT_ID}, then the Azure CLI application ID}

\item{token}{Optional access token or token-provider function. Leave \code{NULL}
to let 'fabricQueryR' use its normal sign-in flow for a Microsoft Fabric
host. A custom \code{livy_url} requires an explicitly supplied token or provider.
HTTPS validation does not prove ownership or token audience; use a custom
host only when your organization controls it, with a credential issued for
its intended audience}

\item{auth_args}{Additional sign-in options passed to
\code{\link[AzureAuth:get_azure_token]{AzureAuth::get_azure_token()}}}

\item{audience}{Optional sign-in scopes. For delegated sign-in, \code{NULL}
requests the four required Livy scopes listed below. An explicit vector
replaces those defaults, so include every required scope plus any optional
\verb{Code.Access*} scope the Spark code needs. Client credentials require one
\code{.default} audience}

\item{verbose}{Logical. Show submission and lifecycle messages}

\item{wait}{Logical. \code{FALSE} returns immediately so other R work can
continue; \code{TRUE} waits for a terminal state before returning the same
object}

\item{timeout}{Maximum seconds to wait when \code{wait = TRUE}}

\item{poll_interval}{Seconds between status checks when waiting}

\item{cancel_on_timeout}{Logical. When waiting at submission time, request
cancellation if the local timeout expires. Defaults to \code{TRUE}, so a timed
out call does not normally leave Spark compute running unattended. The
structured timeout condition contains the live \link{FabricLivyBatch} object in
\code{handle}, for status checks or cancellation in the current R process, and
stable public metadata in \code{batch}. A serialized handle intentionally loses
its in-process credential}
}
\value{
A \link{FabricLivyBatch} 'R6' object. Inspect its \verb{$state}, call
\verb{$result()} for structured metadata and logs, and call \verb{$wait()} later when
submitting with \code{wait = FALSE}
}
\description{
Runs a complete Python, R, or Java/Scala Spark application stored in OneLake
or ADLS. Use this for repeatable scripts and unattended processing; use
\code{\link[=fabric_livy_session]{fabric_livy_session()}} when several interactive statements should share
variables and Spark state
}
\section{Before you submit}{

Fabric needs a workspace on supported capacity and a Lakehouse. The
application file must already be accessible through an ABFS/ABFSS URI; this
function does not upload a local script. Use \code{\link[=fabric_onelake_upload]{fabric_onelake_upload()}} first
when needed

Python and Java batch applications have produced their expected output in
the package's persistent Fabric sandbox. Standalone R batches remain
unverified: attempts have failed during Spark-context initialization. Treat
the R batch path as experimental and validate an application's output in the
target runtime before relying on it. Successful \code{kind = "sparkr"} interactive
statements do not establish standalone R batch support.

Delegated sign-in requires \code{Lakehouse.Execute.All}, \code{Lakehouse.Read.All},
\code{Code.AccessFabric.All}, and \code{Code.AccessStorage.All}. Add
\code{Code.AccessAzureKeyvault.All}, \code{Code.AccessAzureDataLake.All},
\code{Code.AccessAzureDataExplorer.All}, or \code{Code.AccessSQL.All} only when Spark
accesses that Azure service at runtime. The signed-in identity also needs an
appropriate workspace role

Microsoft's current batch guide is internally inconsistent about service
principals: its introduction says SPN is unsupported, while its
authentication section provides a certificate-based SPN example. This
package can acquire and send a client-credentials token, but cannot make the
Fabric service accept that identity. Until Microsoft clarifies the contract,
verify unattended batch authentication in the target tenant and use a
delegated user when the service rejects an SPN. A Contributor role alone is
not a guarantee of batch SPN support
}

\examples{
\dontrun{
# Discover the Lakehouse and Python file used by this batch
workspace <- fabric_workspaces()[[1L]]
lakehouse <- fabric_lakehouses(workspace)[[1L]]
scripts <- fabric_onelake_list(
  workspace,
  lakehouse,
  path = "Files/jobs"
)
script <- scripts[grepl("[.]py$", scripts$path), ][1L, ]
script_uri <- paste0(
  "abfss://", workspace$id, "@onelake.dfs.fabric.microsoft.com/",
  lakehouse$id, "/", script$path[[1L]]
)

# Submit the discovered script and wait for its Spark application to finish
batch <- fabric_livy_batch_submit(
  lakehouse,
  file = script_uri,
  wait = TRUE,
  cancel_on_timeout = TRUE
)
batch$result()
}

}
\seealso{
\href{https://learn.microsoft.com/en-us/fabric/data-engineering/get-started-api-livy-batch}{Microsoft Fabric batch jobs}
}
