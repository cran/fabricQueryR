% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fabric_job_scheduler.R
\name{fabric_job_schedules}
\alias{fabric_job_schedules}
\alias{fabric_job_schedule_create}
\alias{fabric_job_schedule_update}
\alias{fabric_job_schedule_delete}
\title{Manage Microsoft Fabric item schedules}
\usage{
fabric_job_schedules(
  item,
  workspace = NULL,
  job_type = NULL,
  item_type = NULL,
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  auth_args = list(),
  api_base = .fabric_api_base
)

fabric_job_schedule_create(
  item,
  configuration,
  workspace = NULL,
  job_type = NULL,
  item_type = NULL,
  enabled = TRUE,
  execution_data = NULL,
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  auth_args = list(),
  api_base = .fabric_api_base
)

fabric_job_schedule_update(
  item,
  schedule_id,
  configuration = NULL,
  workspace = NULL,
  job_type = NULL,
  item_type = NULL,
  enabled = NULL,
  execution_data = NULL,
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  auth_args = list(),
  api_base = .fabric_api_base
)

fabric_job_schedule_delete(
  item,
  schedule_id,
  workspace = NULL,
  job_type = NULL,
  item_type = NULL,
  confirm = FALSE,
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  auth_args = list(),
  api_base = .fabric_api_base
)
}
\arguments{
\item{item}{Item GUID, exact display name, or an item object returned by a
discovery function. A discovered object is recommended because it includes
the item type and workspace ID.}

\item{workspace}{Workspace GUID, exact display name, or a workspace object.
Omit it when \code{item} is a discovered object containing \code{workspaceId}.}

\item{job_type}{Schedule job type. Notebooks default to \code{"RunNotebook"},
Spark job definitions to \code{"SparkJob"}, and data pipelines, Dataflows, and
Data Build Tool Jobs to \code{"Execute"}.
For a Dataflow publish schedule, set \code{job_type = "ApplyChanges"}
explicitly. Lakehouses require an explicit value because they do not have
a safe generic default; use \code{"RefreshMaterializedLakeViews"} for the
materialized Lake View refresh route and supply its documented
\code{execution_data}. Microsoft labels the Lakehouse materialized Lake View
schedule API as Preview, for evaluation and development only, and does not
recommend it for production use. Unknown item types retain the Core
Scheduler's \code{"DefaultJob"} fallback. Supply an explicit value for another
workload-specific schedule job type. When passing one of these item types
as a GUID instead of a discovered item, also supply \code{item_type} or set the
documented \code{job_type} explicitly.}

\item{item_type}{Optional Fabric item type when \code{item} is a GUID. A discovered
item supplies this automatically.}

\item{tenant_id}{Entra tenant ID. Defaults to
\code{FABRICQUERYR_TENANT_ID}}

\item{client_id}{Entra application ID. Defaults to
\code{FABRICQUERYR_CLIENT_ID}, then the Azure CLI application ID}

\item{token}{Optional access token or token-provider function. Leave \code{NULL}
to let 'fabricQueryR' use its normal sign-in flow
A \code{fabric_job} handle reuses its stored credential unless \code{tenant_id},
\code{client_id}, \code{token}, or non-empty \code{auth_args} is supplied explicitly}

\item{auth_args}{Additional sign-in options passed to
\code{\link[AzureAuth:get_azure_token]{AzureAuth::get_azure_token()}} when no token source is supplied}

\item{api_base}{Fabric REST API base URL. Most users should keep the default
A discovered workspace-specific endpoint is used unless this argument is
supplied explicitly}

\item{configuration}{A value returned by
\code{\link[=fabric_job_schedule_config]{fabric_job_schedule_config()}}. Advanced callers may pass a named list in
the documented Fabric \code{ScheduleConfig} shape. Known types are validated;
an unknown future \code{type} is passed through and remains inspectable.}

\item{enabled}{Whether the schedule is enabled. Fabric can automatically
disable schedules after repeated failures; updating one with
\code{enabled = TRUE} explicitly restarts it.}

\item{execution_data}{Optional named list of static, workload-specific
execution data. Its schema is defined by the item's job type. The package
preserves it without assuming that all workloads share one schema.}

\item{schedule_id}{Schedule GUID, or a \code{fabric_job_schedule} record returned
by a schedule function.}

\item{confirm}{Must be explicitly set to \code{TRUE} before a schedule is deleted.}
}
\value{
\code{fabric_job_schedules()} returns a list of \code{fabric_job_schedule}
records. Create and update return one such record. Delete invisibly returns
\code{TRUE}. Records expose normalized common fields and retain the complete
service response in \code{raw}.
}
\description{
List, create, update, or delete recurring schedules for a supported Fabric
item. Use \code{\link[=fabric_job_schedule_config]{fabric_job_schedule_config()}} to construct the four schedule types
in the current REST contract.
}
\details{
Schedule deletion is not replayed after an ambiguous transport failure. An
already absent schedule is treated as deleted.

List operations need an item read permission. Create and update require item
execute and read-write permissions; delete requires item read-write
permission. The current service limit is 20 schedules per item.

\code{fabric_job_schedule_update()} accepts partial R input for convenience, but
the Fabric PATCH contract requires \code{enabled} and a complete \code{configuration}.
When either is omitted, the function first reads the current schedule and
preserves the omitted value. Omitted \code{configuration} and omitted or \code{NULL}
\code{execution_data} are replayed from the original response JSON, retaining
numeric precision and empty objects or arrays; supply a named list to replace
either value. Decoded record fields use ordinary R JSON types and cannot
represent arbitrary decimals.

The published REST response currently exposes \code{enabled} but no standard
auto-disable reason. \code{auto_disabled} is therefore \code{NA} unless Fabric returns
an explicit marker. The complete response stays available in \code{raw}.

Semantic-model refresh schedules use the Power BI dataset schedule API, not
the Fabric Core Job Scheduler. These functions reject a discovered semantic
model unless \code{job_type} is supplied explicitly for a future or custom route.
}
\examples{
\dontrun{
# Discover the Notebook instead of copying workspace and item IDs
workspace <- fabric_workspaces()[[1L]]
notebook <- fabric_notebooks(workspace)[[1L]]

# Inspect existing schedules before creating another one
existing <- fabric_job_schedules(notebook)

# Build a weekly configuration using Fabric's Windows time-zone name
configuration <- fabric_job_schedule_config(
  "Weekly",
  start_time = "2026-10-01T00:00:00Z",
  end_time = "2027-10-01T00:00:00Z",
  time_zone = "W. Europe Standard Time",
  times = "07:30",
  weekdays = c("Monday", "Thursday")
)

# Create, disable, and finally delete the schedule returned by Fabric
schedule <- fabric_job_schedule_create(notebook, configuration)
fabric_job_schedule_update(notebook, schedule, enabled = FALSE)
fabric_job_schedule_delete(notebook, schedule, confirm = TRUE)
}
}
\references{
\href{https://learn.microsoft.com/en-us/rest/api/fabric/core/job-scheduler/}{Fabric Job Scheduler REST API}

\href{https://learn.microsoft.com/en-us/rest/api/power-bi/datasets/update-refresh-schedule-in-group}{Update a semantic-model refresh schedule}

\href{https://learn.microsoft.com/en-us/rest/api/fabric/datapipeline/background-jobs/schedule-execute}{Schedule a Data Pipeline}

\href{https://learn.microsoft.com/en-us/rest/api/fabric/dataflow/background-jobs/schedule-execute}{Schedule Dataflow Execute}

\href{https://learn.microsoft.com/en-us/rest/api/fabric/dataflow/background-jobs/schedule-apply-changes}{Schedule Dataflow Apply Changes}

\href{https://learn.microsoft.com/en-us/rest/api/fabric/lakehouse/background-jobs/create-refresh-materialized-lake-views-schedule}{Schedule a Lakehouse materialized Lake View refresh}

\href{https://learn.microsoft.com/en-us/rest/api/fabric/databuildtooljob/background-jobs/schedule-data-build-tool-job}{Schedule a Data Build Tool Job}

\href{https://learn.microsoft.com/en-us/fabric/data-factory/pipeline-rest-api-capabilities}{Fabric Data Pipeline REST API capabilities}

\href{https://learn.microsoft.com/en-us/fabric/fundamentals/job-scheduler}{Fabric job scheduler behavior}
}
