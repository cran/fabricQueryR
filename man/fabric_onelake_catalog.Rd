% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fabric_onelake_catalog.R
\name{fabric_onelake_catalog}
\alias{fabric_onelake_catalog}
\alias{fabric_lakehouse_schemas}
\alias{fabric_warehouse_schemas}
\alias{fabric_lakehouse_table}
\alias{fabric_warehouse_table}
\title{Discover OneLake schemas and individual tables}
\usage{
fabric_lakehouse_schemas(
  lakehouse,
  workspace = NULL,
  page_size = NULL,
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  auth_args = list(),
  api_base = .fabric_api_base,
  table_api_base = .fabric_onelake_table_base,
  storage_token = NULL
)

fabric_warehouse_schemas(
  warehouse,
  workspace = NULL,
  page_size = NULL,
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  auth_args = list(),
  api_base = .fabric_api_base,
  table_api_base = .fabric_onelake_table_base,
  storage_token = NULL
)

fabric_lakehouse_table(
  lakehouse,
  table,
  workspace = NULL,
  schema = NULL,
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  auth_args = list(),
  api_base = .fabric_api_base,
  table_api_base = .fabric_onelake_table_base,
  storage_token = NULL,
  enrich_fabric = FALSE
)

fabric_warehouse_table(
  warehouse,
  table,
  workspace = NULL,
  schema = NULL,
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  auth_args = list(),
  api_base = .fabric_api_base,
  table_api_base = .fabric_onelake_table_base,
  storage_token = NULL
)
}
\arguments{
\item{lakehouse}{Lakehouse GUID, exact display name, or one Lakehouse object
returned by \code{\link[=fabric_lakehouses]{fabric_lakehouses()}}.}

\item{workspace}{Workspace GUID, exact display name, or discovered workspace.
Omit it when the item object contains \code{workspaceId}.}

\item{page_size}{Optional maximum schemas requested per metadata page, from
1 to 100. All continuation tokens are followed.}

\item{tenant_id}{Entra tenant ID. Defaults to
\code{FABRICQUERYR_TENANT_ID}.}

\item{client_id}{Entra application ID. Defaults to
\code{FABRICQUERYR_CLIENT_ID}, then the Azure CLI application ID.}

\item{token}{Optional access token or audience-aware token-provider function.}

\item{auth_args}{Additional sign-in options passed to \code{fabric_credential()}.}

\item{api_base}{Fabric REST API base used when an item name or GUID must be
resolved. Most users should keep the default.}

\item{table_api_base}{OneLake Delta table API base URL. Most users should
keep the default.}

\item{storage_token}{Optional separate Azure Storage token or token-provider
function. Supply it when \code{token} is fixed and Fabric item lookup is needed.}

\item{warehouse}{Warehouse GUID, exact display name, or one Warehouse object
returned by \code{\link[=fabric_warehouses]{fabric_warehouses()}}.}

\item{table}{Table name, or a record containing a \code{name}, \code{table}, or
\code{displayName} field. A record can also supply \code{schema}.}

\item{schema}{Schema containing \code{table}. Defaults to the Lakehouse default
schema when available, otherwise \code{"dbo"}.}

\item{enrich_fabric}{For \code{fabric_lakehouse_table()}, also list the Fabric
table inventory to enrich the result. Defaults to \code{FALSE}, so a complete
discovered item needs only a Storage token. Setting \code{TRUE} requires both
Fabric and Storage audiences; use an audience-aware provider or supply
\code{token} and \code{storage_token} separately.}
}
\value{
The schema functions return a tibble with \code{name}, \code{catalog},
\code{full_name}, \code{comment}, \code{owner}, \code{schema_id}, timestamps, and the unmodified
metadata record in \code{raw}. The table functions return one row with the same
columns as \code{\link[=fabric_lakehouse_tables]{fabric_lakehouse_tables()}} or \code{\link[=fabric_warehouse_tables]{fabric_warehouse_tables()}}.
}
\description{
These helpers expose the read-only OneLake Delta table metadata API for
Lakehouses and Warehouses. The schema helpers follow every metadata page.
The singular table helpers retrieve one table's full column metadata without
listing every table in every schema.
}
\section{Permissions}{

The OneLake table API uses the Azure Storage token audience and requires
permission to read the item's tables through OneLake.
}

\examples{
\dontrun{
workspace <- fabric_workspaces()[[1L]]
lakehouse <- fabric_lakehouses(workspace)[[1L]]

schemas <- fabric_lakehouse_schemas(lakehouse)
orders <- fabric_lakehouse_table(lakehouse, "orders", schema = "dbo")
}
}
\references{
\href{https://learn.microsoft.com/en-us/rest/api/fabric/articles/onelakecatalog/overview#explore-tables-within-an-item}{Explore tables with OneLake catalog APIs}

\href{https://learn.microsoft.com/en-us/fabric/onelake/table-apis/delta-table-apis-overview}{OneLake table APIs for Delta}
}
