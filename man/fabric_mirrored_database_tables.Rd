% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fabric_mirrored_database_tables.R
\name{fabric_mirrored_database_tables}
\alias{fabric_mirrored_database_tables}
\alias{fabric_mirrored_database_schemas}
\alias{fabric_mirrored_database_table}
\alias{fabric_mirrored_database_read_table}
\title{Work with Microsoft Fabric mirrored database tables}
\usage{
fabric_mirrored_database_schemas(
  mirrored_database,
  workspace = NULL,
  page_size = NULL,
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  auth_args = list(),
  api_base = .fabric_api_base,
  table_api_base = .fabric_onelake_table_base,
  storage_token = NULL
)

fabric_mirrored_database_tables(
  mirrored_database,
  workspace = NULL,
  schema = NULL,
  detail = TRUE,
  page_size = NULL,
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  auth_args = list(),
  api_base = .fabric_api_base,
  table_api_base = .fabric_onelake_table_base,
  storage_token = NULL
)

fabric_mirrored_database_table(
  mirrored_database,
  table,
  workspace = NULL,
  schema = NULL,
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  auth_args = list(),
  api_base = .fabric_api_base,
  table_api_base = .fabric_onelake_table_base,
  storage_token = NULL
)

fabric_mirrored_database_read_table(
  mirrored_database,
  table,
  workspace = NULL,
  schema = NULL,
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  auth_args = list(),
  version = NULL,
  verbose = TRUE,
  dfs_base = "https://onelake.dfs.fabric.microsoft.com",
  columns = NULL,
  limit = NULL,
  result = c("tibble", "arrow_stream"),
  api_base = .fabric_api_base,
  storage_token = NULL
)
}
\arguments{
\item{mirrored_database}{Mirrored Database GUID, exact display name, or one
object returned by \code{\link[=fabric_mirrored_databases]{fabric_mirrored_databases()}}. A discovered object is
recommended because it contains workspace, OneLake, and SQL details.}

\item{workspace}{Workspace GUID, exact display name, or discovered workspace.
Omit it when \code{mirrored_database} contains \code{workspaceId}.}

\item{page_size}{Optional maximum records requested per OneLake metadata
page, from 1 to 100. All continuation tokens are followed.}

\item{tenant_id}{Entra tenant ID. Defaults to
\code{FABRICQUERYR_TENANT_ID}.}

\item{client_id}{Entra application ID. Defaults to
\code{FABRICQUERYR_CLIENT_ID}, then the Azure CLI application ID.}

\item{token}{Optional access token or audience-aware token-provider function.}

\item{auth_args}{Additional sign-in options passed to \code{fabric_credential()}.}

\item{api_base}{Fabric REST API base used when an item name or GUID must be
resolved. Most users should keep the default.}

\item{table_api_base}{OneLake Delta table API base URL. Most users should
keep the default.}

\item{storage_token}{Optional separate Azure Storage token or token-provider
function. Supply it when \code{token} is fixed and item lookup is needed.}

\item{schema}{Optional schema filter. The singular metadata and read helpers
use a discovered default schema when available, otherwise \code{"dbo"}.}

\item{detail}{Whether table discovery should retrieve column metadata for
every table.}

\item{table}{Table name, or a one-row record containing \code{name} and optionally
\code{schema}.}

\item{version}{Specific Delta table version to read, or \code{NULL} for latest.}

\item{verbose}{Whether to show authentication and read progress.}

\item{dfs_base}{OneLake service address. Most users should keep the default;
a workspace-specific address discovered from Fabric is used when available.}

\item{columns}{Column names to return, or \code{NULL} for all columns.}

\item{limit}{Maximum number of rows to return, or \code{NULL} for all rows.}

\item{result}{\code{"tibble"} or \code{"arrow_stream"} for batch processing.}
}
\value{
\code{fabric_mirrored_database_schemas()} returns the same schema tibble
as \code{\link[=fabric_lakehouse_schemas]{fabric_lakehouse_schemas()}}. The table metadata functions return the
same table tibble as \code{\link[=fabric_warehouse_tables]{fabric_warehouse_tables()}}. The reader returns a
tibble or a single-use \code{nanoarrow_array_stream}.
}
\description{
Discover schemas and Delta tables replicated into a Fabric mirrored
database, retrieve one table's detailed metadata, or read a table directly
from OneLake. The discovery helpers use the read-only OneLake table metadata
API; the reader uses the mirrored Delta log.
}
\section{SQL alternative}{

Mirrored databases also expose a read-only SQL analytics endpoint. Pass a
discovered mirrored database object to \code{\link[=fabric_sql_tables]{fabric_sql_tables()}},
\code{\link[=fabric_sql_read_table]{fabric_sql_read_table()}}, or \code{\link[=fabric_sql_query]{fabric_sql_query()}} when SQL permissions or
SQL views are required.
}

\examples{
\dontrun{
workspace <- fabric_workspaces()[[1L]]
database <- fabric_mirrored_databases(workspace)[[1L]]

schemas <- fabric_mirrored_database_schemas(database)
tables <- fabric_mirrored_database_tables(database)
rows <- fabric_mirrored_database_read_table(database, tables[1L, ], limit = 1000)
}
}
\references{
\href{https://learn.microsoft.com/en-us/rest/api/fabric/mirroreddatabase/items/get-mirrored-database}{Get Mirrored Database}

\href{https://learn.microsoft.com/en-us/fabric/mirroring/overview}{Mirroring in Microsoft Fabric}

\href{https://learn.microsoft.com/en-us/rest/api/fabric/articles/onelakecatalog/overview#explore-tables-within-an-item}{OneLake catalog table APIs}
}
