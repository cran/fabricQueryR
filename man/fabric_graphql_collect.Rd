% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fabric_graphql_query.R
\name{fabric_graphql_collect}
\alias{fabric_graphql_collect}
\title{Collect paged GraphQL row objects into a tibble}
\usage{
fabric_graphql_collect(pages, path)
}
\arguments{
\item{pages}{A \code{fabric_graphql_pages} result from
\code{\link[=fabric_graphql_paginate]{fabric_graphql_paginate()}}. Requiring that result, rather than an
unverified single response, lets the function enforce completion}

\item{path}{Character path from each page's \code{data} field to the list of row
objects, for example \code{c("viewer", "products", "items")}}
}
\value{
A \code{fabric_graphql_rows} tibble. Attributes \code{complete}, \code{errors},
\code{page_count}, \code{path}, and \code{null_rows} retain collection metadata
}
\description{
Combines row objects from a caller-selected field in every result returned
by \code{\link[=fabric_graphql_paginate]{fabric_graphql_paginate()}}. The explicit \code{path} is relative to each
page's \code{data} field because GraphQL response shapes are schema-defined and
cannot be inferred safely
}
\details{
Scalar fields become ordinary tibble columns. Nested objects and arrays stay
as list-columns and are never flattened. Fields introduced on later pages
are added in first-seen order, with missing or GraphQL \code{null} scalar values
represented by typed \code{NA} values when their type can be inferred. Exact
integer strings returned by \code{\link[=fabric_graphql_query]{fabric_graphql_query()}} remain character data;
finite numeric entries in the same field, including fractions, are promoted
to character using text that recovers the received R value exactly. Fields
containing only numeric values retain ordinary numeric columns. Nullable row
elements retain their positions as missing rows; the \code{null_rows} attribute
records their one-based positions, distinguishing them from objects whose
fields are all null.

A successful result has class \code{fabric_graphql_rows} and reports completion,
page count, path, and GraphQL errors in its printed header and attributes.
Use \code{attr(rows, "errors")} to inspect partial GraphQL errors. If pagination
stopped before \code{next_cursor} reported completion, including at \code{max_pages},
the function raises \code{fabric_graphql_collection_error}; its \code{partial_data}
field contains the rows collected so far and is explicitly marked
incomplete
}
\examples{
\dontrun{
# Discover the GraphQL API, then fetch every Products page
workspace <- fabric_workspaces()[[1L]]
api <- fabric_graphql_apis(workspace)[[1L]]
pages <- fabric_graphql_paginate(
  api,
  query = paste(
    "query Products($first: Int!, $after: String) {",
    "  products(first: $first, after: $after) {",
    "    items { id name details { category } }",
    "    hasNextPage endCursor",
    "  }",
    "}"
  ),
  variables = list(first = 100L, after = NULL),
  next_cursor = fabric_graphql_cursor("products")
)

# Combine nested item rows from every page into one tibble
rows <- fabric_graphql_collect(pages, c("products", "items"))
attr(rows, "complete")
attr(rows, "errors")
}
}
\references{
\href{https://learn.microsoft.com/en-us/fabric/data-engineering/api-graphql-limits}{Fabric API for GraphQL limits}

\href{https://learn.microsoft.com/en-us/fabric/data-engineering/api-graphql-aggregations}{Fabric GraphQL aggregation and pagination shape}
}
