% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fabric_warehouse_tables.R
\name{fabric_warehouse_read_table}
\alias{fabric_warehouse_read_table}
\title{Read a Microsoft Fabric Warehouse table}
\usage{
fabric_warehouse_read_table(
  warehouse,
  table,
  workspace = NULL,
  schema = "dbo",
  columns = NULL,
  limit = NULL,
  result = c("tibble", "arrow_stream"),
  backend = c("odbc", "adbc"),
  numeric_policy = c("auto", "exact", "driver"),
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  auth_args = list(),
  api_base = .fabric_api_base,
  verbose = TRUE,
  timeout = 30L,
  max_tries = 3L,
  retry_delay = 5,
  sql_token = NULL
)
}
\arguments{
\item{warehouse}{A Warehouse object returned by \code{\link[=fabric_warehouses]{fabric_warehouses()}} or
\code{\link[=fabric_item]{fabric_item()}}, or its name or GUID when \code{workspace} is supplied.}

\item{table}{Warehouse table name, or a record containing a \code{name}, \code{table},
or \code{displayName} field.}

\item{workspace}{Workspace name, GUID, or discovery object containing
\code{warehouse}. May be omitted when \code{warehouse} is a discovery object.}

\item{schema}{Warehouse schema. Defaults to \code{"dbo"}; a table record can
supply its schema when this argument is omitted.}

\item{columns}{Optional unique column names to project.}

\item{limit}{Optional non-negative maximum number of rows to return.}

\item{result}{Return a \code{"tibble"} for ordinary R analysis, or a single-use
\code{"arrow_stream"}. ADBC streams retain native Arrow types. ODBC streams are
converted from R data frames and cannot recover values lost by the driver.
The 'adbi' driver may fetch the complete result before returning
the stream, so this option does not guarantee bounded-memory retrieval.
An Arrow stream owns its DBI result and connection until the stream is
released; consume it promptly or release it explicitly with
\code{\link[nanoarrow:nanoarrow_pointer_release]{nanoarrow::nanoarrow_pointer_release()}}}

\item{backend}{SQL connection backend, \code{"odbc"} or \code{"adbc"}.}

\item{numeric_policy}{\code{"auto"} (default) uses \code{"driver"} for ODBC and
\code{"exact"} for ADBC. Automatic ODBC conversion warns once per R session
about possible numeric precision loss. Set \code{"driver"} explicitly to
accept the driver's conversions without this warning, or use \code{"exact"}
to reject unsafe ODBC results before fetching

\code{"exact"} preserves ADBC decimals as character and BIGINT as
\code{bit64::integer64}, using character for columns
containing the minimum BIGINT. INT columns containing \code{-2147483648} use
exact doubles. Nested lists retain character decimals and 64-bit integers,
and double 32-bit integers. Null struct parents require
\code{result = "arrow_stream"}; exact tibble collection raises
\code{fabric_arrow_null_struct_error} to preserve their distinction from valid
structs with all-null fields. ODBC rejects DECIMAL, NUMERIC, INT and BIGINT
columns before fetching: its conversion can round or truncate values
or turn valid integer boundaries into missing values. Cast these columns
to \code{varchar} in SQL or use ADBC. \code{"driver"} explicitly accepts the backend's
conversions, including possible rounding and missing values, for either
output format. This policy applies to this query helper; direct DBI calls
on \code{\link[=fabric_sql_connect]{fabric_sql_connect()}} use the selected driver's conversion settings}

\item{tenant_id}{Microsoft Entra tenant ID. Defaults to
\code{FABRICQUERYR_TENANT_ID}}

\item{client_id}{Microsoft Entra application/client ID. Defaults to
\code{FABRICQUERYR_CLIENT_ID}, then the Azure CLI application ID}

\item{token}{Optional access token or token-provider function. Leave \code{NULL}
to let 'fabricQueryR' use its normal sign-in flow}

\item{auth_args}{Additional sign-in options passed to
\code{\link[AzureAuth:get_azure_token]{AzureAuth::get_azure_token()}}}

\item{api_base}{Fabric REST API base used when a Warehouse name or GUID must
be discovered.}

\item{verbose}{Whether to report SQL connection progress.}

\item{timeout}{Non-negative whole-number login/connect timeout in seconds;
\code{0} lets the driver use an unlimited or driver-specific timeout}

\item{max_tries}{Maximum attempts after temporary Fabric SQL failures}

\item{retry_delay}{Initial delay in seconds before retrying. Later retries
wait progressively longer, up to 60 seconds}

\item{sql_token}{Optional separate Azure SQL token or token-provider
function. Supply it when \code{token} is fixed rather than audience-aware.}
}
\value{
A tibble, or a single-use \code{nanoarrow_array_stream} when
\code{result = "arrow_stream"}.
}
\description{
Provides the table-oriented read counterpart to
\code{\link[=fabric_warehouse_write_table]{fabric_warehouse_write_table()}}. It resolves the Warehouse like the writer,
safely quotes the schema, table, and projected columns, and delegates query
execution and type conversion to \code{\link[=fabric_sql_query]{fabric_sql_query()}}. Use that lower-level
function for filters, ordering, joins, aggregations, or other T-SQL.
}
\section{Large results}{

Use \code{backend = "adbc"} with \code{result = "arrow_stream"} for a native Arrow
result path that avoids conversion to an R data frame. The current result path
through 'DBI' and 'adbi' may fetch the complete result before returning the
stream, so use a selective query or \code{limit} when the result may exceed memory.
The external ADBC \code{mssql} driver must be installed.

\code{limit} uses T-SQL \code{TOP} and does not define row order. Use
\code{\link[=fabric_sql_query]{fabric_sql_query()}} with an explicit \verb{ORDER BY} when deterministic row
selection matters.
}

\examples{
\dontrun{
# Discover the Warehouse instead of copying its SQL connection details
workspace <- fabric_workspaces()[[1L]]
warehouse <- fabric_warehouses(workspace)[[1L]]

# Use 'DBI' metadata to discover an existing table in that Warehouse
con <- fabric_sql_connect(warehouse)
tables <- DBI::dbListTables(con)
DBI::dbDisconnect(con)
table <- tables[[1L]]

# Read a bounded selection into a tibble
orders <- fabric_warehouse_read_table(
  warehouse,
  table,
  backend = "adbc",
  limit = 1000
)

# Keep the result Arrow-native rather than converting it to a data frame
stream <- fabric_warehouse_read_table(
  warehouse,
  table,
  backend = "adbc",
  result = "arrow_stream"
)
reader <- arrow::as_record_batch_reader(stream)
}
}
\references{
\href{https://learn.microsoft.com/en-us/fabric/data-warehouse/query-warehouse}{Query a Fabric Warehouse}

\href{https://learn.microsoft.com/en-us/fabric/data-warehouse/connectivity}{Fabric Warehouse connectivity}
}
