% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fabric_job_scheduler.R
\name{fabric_job_schedule_config}
\alias{fabric_job_schedule_config}
\title{Build a Microsoft Fabric job schedule configuration}
\usage{
fabric_job_schedule_config(
  type = "Cron",
  start_time,
  end_time,
  time_zone = "UTC",
  interval = NULL,
  times = NULL,
  weekdays = NULL,
  recurrence = NULL,
  day_of_month = NULL,
  week_index = NULL,
  weekday = NULL
)
}
\arguments{
\item{type}{Schedule type: \code{"Cron"} for a minute interval, \code{"Daily"},
\code{"Weekly"}, or \code{"Monthly"}.}

\item{start_time, end_time}{A scalar \code{POSIXt} value or RFC 3339 string with an
explicit \code{Z} or numeric offset. These boundaries are converted to UTC.
Fractional seconds are rejected; round or truncate explicitly before use.}

\item{time_zone}{Windows time-zone identifier used to interpret \code{times}, such
as \code{"UTC"}, \code{"W. Europe Standard Time"}, or
\code{"Central Standard Time"}. Fabric validates the identifier.}

\item{interval}{For a \code{Cron} schedule, a whole-number interval in minutes
from 1 through 5,270,400.}

\item{times}{For daily, weekly, and monthly schedules, one or more local clock
times in 24-hour \code{"HH:MM"} form. The REST contract permits up to 100.}

\item{weekdays}{For a weekly schedule, one or more English weekday names.}

\item{recurrence}{For a monthly schedule, the whole-number month interval
from 1 through 12.}

\item{day_of_month}{For a monthly schedule, a day from 1 through 31. Invalid
dates in a particular month are skipped by Fabric. Supply this or the
\code{week_index} and \code{weekday} pair.}

\item{week_index}{For an ordinal monthly schedule, one of \code{"First"} through
\code{"Fifth"}.}

\item{weekday}{For an ordinal monthly schedule, one English weekday name.}
}
\value{
A named list using the Fabric \code{ScheduleConfig} JSON field names.
}
\description{
Creates a validated configuration for \code{\link[=fabric_job_schedule_create]{fabric_job_schedule_create()}} or
\code{\link[=fabric_job_schedule_update]{fabric_job_schedule_update()}}. Recurrence clock times use the supplied
Windows time-zone identifier, while schedule boundaries are sent to Fabric in
UTC.
}
\details{
A Cron schedule is Fabric's minute-interval schedule; this function does not
accept a cron expression because the REST API does not use one. Daylight
saving behavior is controlled by Fabric using \code{time_zone}, not by the R
process's local time zone. Arguments that do not belong to the selected
documented schedule type are rejected.

Non-UTC firing remains unverified in the package's persistent Fabric sandbox:
enabled Amsterdam schedules have not produced the expected run within the
test window. The cause has not been established. Monthly recurrence and
nonexistent or repeated local times at daylight-saving transitions also lack
live execution evidence. Verify a scheduled run through
\code{\link[=fabric_job_instances]{fabric_job_instances()}} in the target workspace before relying on these
configurations; successful schedule creation only confirms API acceptance.
}
\examples{
# Describe a schedule in the Windows time zone used by Fabric
daily <- fabric_job_schedule_config(
  "Daily",
  start_time = "2026-10-01T00:00:00Z",
  end_time = "2027-10-01T00:00:00Z",
  time_zone = "W. Europe Standard Time",
  times = c("08:30", "17:00")
)
}
\references{
\href{https://learn.microsoft.com/en-us/rest/api/fabric/core/job-scheduler/create-item-schedule}{Create item schedule}

\href{https://learn.microsoft.com/en-us/windows-hardware/manufacture/desktop/default-time-zones}{Windows default time zones}
}
