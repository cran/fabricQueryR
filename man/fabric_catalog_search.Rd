% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fabric_catalog.R
\name{fabric_catalog_search}
\alias{fabric_catalog_search}
\title{Search the OneLake catalog}
\usage{
fabric_catalog_search(
  search = NULL,
  types = NULL,
  filter = NULL,
  page_size = NULL,
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  auth_args = list(),
  api_base = .fabric_api_base,
  output = c("r6", "list")
)
}
\arguments{
\item{search}{Optional non-empty text query. Fabric searches display names,
workspace display names, and descriptions. Leave \code{NULL} to browse visible
catalog entries without a text query.}

\item{types}{Optional unique vector of Fabric item types. This is converted
to the catalog API's documented \verb{Type eq ... or Type eq ...} filter.}

\item{filter}{Optional raw catalog filter string. Fabric currently supports
\code{Type}, \code{eq}, \code{ne}, \code{or}, and parentheses. Supply either \code{types} or
\code{filter}, not both.}

\item{page_size}{Optional number of results requested per page, from 1 to
1000. Leave \code{NULL} to use Fabric's service default.}

\item{tenant_id}{Microsoft Entra tenant ID. Defaults to
\code{FABRICQUERYR_TENANT_ID}}

\item{client_id}{Microsoft Entra application/client ID. Defaults to
\code{FABRICQUERYR_CLIENT_ID}, then the Azure CLI application ID}

\item{token}{Optional access token or token-provider function. Leave \code{NULL}
to let 'fabricQueryR' use its normal sign-in flow}

\item{auth_args}{Additional sign-in options passed to
\code{\link[AzureAuth:get_azure_token]{AzureAuth::get_azure_token()}}}

\item{api_base}{Fabric REST API base URL. Leave unchanged unless using a
different Fabric cloud or a test service}

\item{output}{Discovery record representation. The default \code{"r6"} returns
R6 objects with type-specific methods. Use \code{"list"} when a plain record is
specifically required}
}
\value{
With \code{output = "r6"}, a list of \link{FabricItem} objects or
type-specific subclasses. With \code{output = "list"}, a list of
\code{fabric_catalog_entry} records that also inherit from \code{fabric_item}. Both
representations preserve the fields returned by Fabric and add the item
workspace identity from the catalog hierarchy.
Workspace entries returned by unfiltered browsing become \link{FabricWorkspace}
objects, or \code{fabric_catalog_entry} records inheriting \code{fabric_workspace}.
}
\description{
Searches Fabric item metadata across every workspace visible to the caller.
Results are lightweight R6 discovery objects that contain the item and
workspace identity. Type-specific results expose the methods that can run
from that metadata.
}
\details{
Catalog search is a preview Fabric API. It is for metadata discovery only
and does not grant access to item contents. The caller needs
\code{Catalog.Read.All}; Fabric returns only entries that the calling user,
service principal, or managed identity is authorized to see.

Pagination sends the search parameters only on the first request and sends
only the continuation token on later requests, as required by Fabric. A
repeated or malformed token raises a \code{fabric_catalog_protocol_error} instead
of silently returning partial results or looping indefinitely.
}
\examples{
\dontrun{
# Search all visible workspaces for Lakehouses related to sales
entries <- fabric_catalog_search(
  search = "sales",
  types = "Lakehouse",
  page_size = 100
)

# `$onelake_list()` calls fabric_onelake_list()
lakehouse <- entries[[1L]]
lakehouse$onelake_list(path = "Tables")
}
}
\references{
\href{https://learn.microsoft.com/en-us/rest/api/fabric/core/catalog/search}{Catalog search REST API}

\href{https://learn.microsoft.com/en-us/rest/api/fabric/articles/onelakecatalog/overview}{OneLake Catalog REST API overview}
}
