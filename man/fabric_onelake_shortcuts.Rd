% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fabric_onelake_shortcuts.R
\name{fabric_onelake_shortcuts}
\alias{fabric_onelake_shortcuts}
\alias{fabric_onelake_shortcut_get}
\alias{fabric_onelake_shortcut_create}
\alias{fabric_onelake_shortcuts_bulk_create}
\alias{fabric_onelake_shortcut_cache_reset}
\alias{fabric_onelake_shortcut_delete}
\title{Manage OneLake shortcuts}
\usage{
fabric_onelake_shortcuts(
  item,
  workspace = NULL,
  item_type = NULL,
  parent_path = NULL,
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  auth_args = list(),
  api_base = .fabric_api_base
)

fabric_onelake_shortcut_get(
  item,
  path,
  name,
  workspace = NULL,
  item_type = NULL,
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  auth_args = list(),
  api_base = .fabric_api_base
)

fabric_onelake_shortcut_create(
  item,
  path,
  name,
  target,
  workspace = NULL,
  item_type = NULL,
  target_workspace = NULL,
  target_path = NULL,
  target_item_type = NULL,
  conflict_policy = c("Abort", "GenerateUniqueName", "CreateOrOverwrite",
    "OverwriteOnly"),
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  auth_args = list(),
  api_base = .fabric_api_base
)

fabric_onelake_shortcuts_bulk_create(
  item,
  shortcuts,
  workspace = NULL,
  item_type = NULL,
  conflict_policy = c("Abort", "GenerateUniqueName", "CreateOrOverwrite",
    "OverwriteOnly"),
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  auth_args = list(),
  api_base = .fabric_api_base
)

fabric_onelake_shortcut_cache_reset(
  workspace,
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  auth_args = list(),
  api_base = .fabric_api_base
)

fabric_onelake_shortcut_delete(
  item,
  path,
  name,
  workspace = NULL,
  item_type = NULL,
  confirm = FALSE,
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  auth_args = list(),
  api_base = .fabric_api_base
)
}
\arguments{
\item{item}{Destination Fabric item name, GUID, or object returned by a
discovery function.}

\item{workspace}{Workspace name, GUID, or discovery object containing
\code{item}. May be omitted when \code{item} contains \code{workspaceId}.}

\item{item_type}{Optional item type used to disambiguate a destination item
supplied by name.}

\item{parent_path}{Optional \code{Files} or \code{Tables} path from which listing
starts. Fabric still returns shortcuts below that path exhaustively.}

\item{tenant_id}{Microsoft Entra tenant ID. Defaults to
\code{FABRICQUERYR_TENANT_ID}}

\item{client_id}{Microsoft Entra application/client ID. Defaults to
\code{FABRICQUERYR_CLIENT_ID}, then the Azure CLI application ID}

\item{token}{Optional access token or token-provider function. Leave \code{NULL}
to let 'fabricQueryR' use its normal sign-in flow}

\item{auth_args}{Additional sign-in options passed to
\code{\link[AzureAuth:get_azure_token]{AzureAuth::get_azure_token()}}}

\item{api_base}{Fabric REST API base URL. Leave unchanged unless using a
different Fabric cloud or a test service}

\item{path}{Parent \code{Files} or \code{Tables} path where the shortcut exists or will
be created. Local checks validate path syntax, while Fabric applies
item- and workload-specific placement rules. For a table shortcut in a
Lakehouse without schemas, use \code{Tables}. In a schema-enabled Lakehouse,
use a schema path such as \code{Tables/dbo}. A schema shortcut instead lives
under \code{Tables} and targets a folder containing multiple Delta tables.}

\item{name}{Shortcut name.}

\item{target}{A discovered Fabric item, its name or GUID, or a raw named
shortcut target list. A raw target must contain exactly one documented
key such as \code{oneLake}, \code{adlsGen2}, \code{amazonS3}, \code{azureBlobStorage},
\code{googleCloudStorage}, \code{oneDriveSharePoint}, \code{s3Compatible}, or \code{dataverse}.
Connection-backed targets must include the documented required fields, use
a Fabric connection ID, and must not embed credentials. Local checks cover
structure, required fields, identifier shape, and generic URL safety; they
do not verify source-specific host/path rules or that a connection refers
to the supplied location. A raw \code{oneLake} target may also include its
documented optional \code{connectionId}. Fabric service validation is
authoritative.}

\item{target_workspace}{Workspace containing a OneLake \code{target}. May be
omitted when a discovered target contains \code{workspaceId}.}

\item{target_path}{Item-relative \code{Files} or \code{Tables} path for a OneLake
target. Required when \code{target} is an item and unused for a raw target list.}

\item{target_item_type}{Optional Fabric item type used to disambiguate a
OneLake target supplied by name.}

\item{conflict_policy}{\code{"Abort"} preserves an existing shortcut with the
same path and name. \code{"GenerateUniqueName"} creates a uniquely named
shortcut, \code{"CreateOrOverwrite"} creates or updates it, and
\code{"OverwriteOnly"} updates an existing shortcut without creating one.}

\item{shortcuts}{For bulk creation, a non-empty list of shortcut request
lists. Each request requires \code{path}, \code{name}, and \code{target}, accepts the same
target companion fields as \code{fabric_onelake_shortcut_create()}, and may
include a \code{transform} list. The only currently documented transform is
\code{csvToDelta}; see Details.}

\item{confirm}{Logical. Deletion is disabled unless explicitly set to
\code{TRUE}. Deleting a shortcut does not delete its destination data.}
}
\value{
\code{fabric_onelake_shortcuts()} returns a tibble with one row per
shortcut. \code{fabric_onelake_shortcut_get()} and
\code{fabric_onelake_shortcut_create()} return the same one-row shape.
\code{fabric_onelake_shortcut_delete()} returns \code{TRUE} invisibly after success.
\code{fabric_onelake_shortcut_cache_reset()} returns a \code{fabric_operation}
handle for either immediate or asynchronous completion.
}
\description{
Lists, inspects, creates or updates, and deletes shortcuts on a Fabric item.
Discovered Fabric items can be used directly as OneLake targets. A validated
raw target list supports connection-backed shortcut types already configured
in Fabric without copying data into R.
}
\details{
Shortcut names, parent paths, and OneLake target paths follow Fabric's
current shortcut limits: \verb{\%}, \code{+}, and non-ASCII characters are rejected.
Other source- and destination-specific restrictions are intentionally left
to Fabric so that newly supported connection types and rules remain usable.

For example, a table shortcut named \code{orders} uses \code{path = "Tables"} in a
Lakehouse without schemas, or \code{path = "Tables/dbo"} in a Lakehouse with
schemas; its \code{target_path} identifies one Delta table. A schema shortcut
named \code{sales} uses \code{path = "Tables"} and a target such as \code{Tables/sales}
containing multiple Delta tables. File shortcuts use \code{Files} or a folder
beneath it and do not register tables.

Listing follows Fabric continuation links and tokens until every shortcut
below \code{parent_path} is returned. Unknown target details and transform fields
are preserved in list columns for forward compatibility.

Create is deliberately not replayed automatically because its POST outcome
can be ambiguous after a transport failure. The default conflict policy is
Fabric's non-destructive \code{Abort}; overwrite must be requested explicitly.
Deletion is also not replayed automatically, and a 404 confirms that the
shortcut link is already absent.

Bulk creation is a preview Fabric API. Its optional \code{csvToDelta} transform
accepts \code{includeSubfolders} and a \code{properties} list containing \code{delimiter},
\code{skipFilesWithErrors}, and \code{useFirstRowAsHeader}. Supported delimiters are
comma, space, tab, \code{|}, \code{&}, and \verb{;}. A bulk request returns a
\code{fabric_operation}; use \code{\link[=fabric_operation_result]{fabric_operation_result()}} to retrieve the
per-request statuses, created shortcuts, and errors after completion.

These Core REST APIs require \code{OneLake.Read.All} or
\code{OneLake.ReadWrite.All} for reads, and \code{OneLake.ReadWrite.All} for create and
delete. Fabric documents support for users, service principals, and managed
identities. API scope is not sufficient by itself: listing or reading also
requires item Read permission or OneLake Read permission on the destination
path. Creating requires item Write or OneLake ReadWrite on the destination,
plus Read access to the target path. Updating or deleting likewise requires
item Write or destination-path OneLake ReadWrite permission.
}
\examples{
\dontrun{
# Discover two Lakehouses in the same workspace
workspace <- fabric_workspaces()[[1L]]
lakehouses <- fabric_lakehouses(workspace)
destination <- lakehouses[[1L]]
source <- lakehouses[[2L]]
source_paths <- fabric_onelake_list(workspace, source, path = "Tables")
source_table <- source_paths[source_paths$is_directory, ][1L, ]

# Create a shortcut whose target came from the source Lakehouse listing
created <- fabric_onelake_shortcut_create(
  destination,
  path = "Files",
  name = "shared-orders",
  target = source,
  target_path = source_table$path[[1L]]
)

# List the folder, then fetch the created shortcut by its returned identity
fabric_onelake_shortcuts(destination, parent_path = "Files")
shortcut <- fabric_onelake_shortcut_get(
  destination,
  path = created$path[[1L]],
  name = created$name[[1L]]
)

# Delete that same discovered shortcut explicitly
fabric_onelake_shortcut_delete(
  destination,
  path = created$path[[1L]],
  name = created$name[[1L]],
  confirm = TRUE
)
}
}
\references{
\href{https://learn.microsoft.com/en-us/rest/api/fabric/core/onelake-shortcuts/}{OneLake shortcuts REST API}

\href{https://learn.microsoft.com/en-us/fabric/onelake/onelake-shortcuts}{OneLake shortcut placement and limitations}

\href{https://learn.microsoft.com/en-us/fabric/onelake/create-onelake-shortcut}{Create table and schema shortcuts}

\href{https://learn.microsoft.com/en-us/fabric/onelake/onelake-shortcut-security}{OneLake shortcut security and path permissions}

\href{https://learn.microsoft.com/en-us/rest/api/fabric/core/onelake-shortcuts/creates-shortcuts-in-bulk}{Create shortcuts in bulk}

\href{https://learn.microsoft.com/en-us/rest/api/fabric/core/onelake-shortcuts/reset-shortcut-cache}{Reset shortcut cache}
}
