% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fabric_sql_tables.R
\name{fabric_sql_tables}
\alias{fabric_sql_tables}
\alias{fabric_sql_views}
\alias{fabric_sql_read_table}
\title{Discover and read tables through a Fabric SQL endpoint}
\usage{
fabric_sql_tables(
  server,
  schema = NULL,
  detail = TRUE,
  database = NULL,
  target_type = c("auto", "lakehouse", "warehouse", "sql_database",
    "sql_analytics_endpoint"),
  backend = c("odbc", "adbc"),
  token = NULL,
  ...
)

fabric_sql_views(
  server,
  schema = NULL,
  detail = TRUE,
  database = NULL,
  target_type = c("auto", "lakehouse", "warehouse", "sql_database",
    "sql_analytics_endpoint"),
  backend = c("odbc", "adbc"),
  token = NULL,
  ...
)

fabric_sql_read_table(
  server,
  table,
  schema = NULL,
  columns = NULL,
  limit = NULL,
  result = c("tibble", "arrow_stream"),
  database = NULL,
  target_type = c("auto", "lakehouse", "warehouse", "sql_database",
    "sql_analytics_endpoint"),
  backend = c("odbc", "adbc"),
  token = NULL,
  ...
)
}
\arguments{
\item{server}{Fabric SQL endpoint, portal connection string, or discovered
SQL-capable item object.}

\item{schema}{Optional schema filter. \code{fabric_sql_read_table()} defaults to
the schema in a discovered table row, otherwise \code{"dbo"}.}

\item{detail}{Whether table or view discovery should retrieve column
metadata.}

\item{database}{Optional catalog/database. An explicit value overrides one
discovered from \code{server}.}

\item{target_type}{Kind of Fabric SQL target. Keep \code{"auto"} unless a custom
hostname prevents automatic identification.}

\item{backend}{SQL driver backend, either \code{"odbc"} or \code{"adbc"}.}

\item{token}{Optional SQL access token or audience-aware token-provider
function.}

\item{...}{Additional authentication, driver, endpoint, timeout, verbosity,
and retry options passed to \code{\link[=fabric_sql_query]{fabric_sql_query()}}. Query text, parameters,
read-only status, and idempotency are controlled by these helpers and
cannot be supplied here.}

\item{table}{Table or view name, or a one-row data frame or named list
containing \code{name} and optionally \code{schema}.}

\item{columns}{Optional unique column names to project.}

\item{limit}{Optional non-negative maximum number of rows to return.}

\item{result}{Result representation for \code{fabric_sql_read_table()}; either a
tibble or a single-use Arrow stream.}
}
\value{
\code{fabric_sql_tables()} and \code{fabric_sql_views()} return a tibble with
object \code{name}, \code{schema}, \code{full_name}, \code{type}, optional view \code{definition},
list-column \code{columns}, and the unmodified discovery row in \code{raw}.
\code{fabric_sql_read_table()} returns a tibble or \code{nanoarrow_array_stream}.
}
\description{
These helpers provide a target-independent metadata and table-read layer for
Fabric SQL endpoints. They accept Lakehouse, Warehouse, Warehouse snapshot,
and SQL Database objects, or the same direct server inputs as
\code{\link[=fabric_sql_query]{fabric_sql_query()}}. Discovery uses SQL catalog metadata views and is
limited by the caller's SQL metadata permissions. Each discovery call uses
one query, including column metadata when \code{detail = TRUE}, on one connection
per attempt. The connection closes before the result is returned.
}
\examples{
\dontrun{
workspace <- fabric_workspaces()[[1L]]
warehouse <- fabric_warehouses(workspace)[[1L]]

tables <- fabric_sql_tables(warehouse, schema = "dbo")
rows <- fabric_sql_read_table(warehouse, tables[1L, ], limit = 1000)
views <- fabric_sql_views(warehouse)
}
}
\references{
\href{https://learn.microsoft.com/en-us/sql/relational-databases/system-information-schema-views/system-information-schema-views-transact-sql}{System information schema views}

\href{https://learn.microsoft.com/en-us/sql/relational-databases/system-catalog-views/sys-sql-modules-transact-sql}{SQL module definitions}

\href{https://learn.microsoft.com/en-us/fabric/data-engineering/lakehouse-sql-analytics-endpoint}{Fabric SQL analytics endpoints}
}
