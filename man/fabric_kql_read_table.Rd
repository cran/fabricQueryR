% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fabric_kql_query.R
\name{fabric_kql_read_table}
\alias{fabric_kql_read_table}
\title{Read a Microsoft Fabric KQL table}
\usage{
fabric_kql_read_table(
  cluster,
  table,
  database = NULL,
  columns = NULL,
  limit = NULL,
  request_properties = list(),
  timeout = 60,
  retain_raw_frames = FALSE,
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  auth_args = list()
)
}
\arguments{
\item{cluster}{Query URI, or one Eventhouse or
KQLDatabase object returned by \code{\link[=fabric_eventhouses]{fabric_eventhouses()}},
\code{\link[=fabric_kql_databases]{fabric_kql_databases()}}, or \code{\link[=fabric_item]{fabric_item()}}. A KQLDatabase object also
supplies \code{database}. Despite the argument name, use Fabric's \strong{Query URI}
here}

\item{table}{KQL table name, or a record containing a \code{name}, \code{table}, or
\code{displayName} field.}

\item{database}{KQL database display name. Supply it with a copied Query URI
or an Eventhouse object; omit it when \code{cluster} is a KQLDatabase object}

\item{columns}{Optional unique column names to project.}

\item{limit}{Optional non-negative whole-number maximum number of rows to
return, no greater than Kusto's signed 32-bit \code{take} limit.}

\item{request_properties}{Named list of Kusto client request options, such as
\code{servertimeout = "2m"} or \code{notruncation = TRUE}. Most users can leave this
empty; these are server-side Kusto controls, not query parameters. Fabric
does not support \code{queryconsistency} or
\code{query_weakconsistency_session_id}}

\item{timeout}{Positive client-side HTTP timeout in seconds. This is separate
from the Kusto \code{servertimeout} request property}

\item{retain_raw_frames}{Logical. Attach the complete decoded Kusto frame
response as \code{kusto_raw_frames}. Keep \code{FALSE} for normal queries to avoid
retaining a second copy of large result data, including on partial-error
conditions}

\item{tenant_id}{Microsoft Entra tenant ID. Defaults to
\code{FABRICQUERYR_TENANT_ID}}

\item{client_id}{Microsoft Entra application/client ID. Defaults to
\code{FABRICQUERYR_CLIENT_ID}, with the Azure CLI application ID as fallback}

\item{token}{Optional access token or token-provider function. Leave \code{NULL}
to let 'fabricQueryR' use its normal sign-in flow}

\item{auth_args}{Additional sign-in options passed to
\code{\link[AzureAuth:get_azure_token]{AzureAuth::get_azure_token()}}}
}
\value{
A typed tibble containing the selected table rows. Kusto metadata is
retained in the same attributes as \code{\link[=fabric_kql_query]{fabric_kql_query()}}.
}
\description{
Provides the table-oriented read counterpart to \code{\link[=fabric_kql_write_table]{fabric_kql_write_table()}}.
It safely resolves the table through Kusto's \code{table()} function, optionally
projects columns and limits rows, then delegates typed result handling to
\code{\link[=fabric_kql_query]{fabric_kql_query()}}. Use that lower-level function for filters, ordering,
joins, aggregations, or other KQL expressions.
}
\section{Large results}{

The Kusto query HTTP response is collected and decoded in R. Use \code{columns}
and \code{limit} to bound an interactive read. For a result too large for client
memory, use \code{\link[=fabric_kql_export]{fabric_kql_export()}} to export it server-side to OneLake or
another supported storage destination.
}

\examples{
\dontrun{
# Discover a KQL database instead of copying its Query URI and name
workspace <- fabric_workspaces()[[1L]]
database <- fabric_kql_databases(workspace)[[1L]]

# Choose an existing table shown under Tables in the Fabric KQL explorer
table <- Sys.getenv("FABRIC_KQL_TABLE")

# Read a bounded portion of that table into a tibble
events <- fabric_kql_read_table(
  database,
  table,
  limit = 1000
)
}
}
\references{
\href{https://learn.microsoft.com/en-us/kusto/query/table-function?view=microsoft-fabric}{Kusto \code{table()} function}

\href{https://learn.microsoft.com/en-us/kusto/query/take-operator?view=microsoft-fabric}{Kusto \code{take} operator}

\href{https://learn.microsoft.com/en-us/kusto/query/schema-entities/entity-names?view=microsoft-fabric}{Kusto entity names}

\href{https://learn.microsoft.com/en-us/kusto/api/rest/request?view=microsoft-fabric}{Kusto query HTTP request}
}
