% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fabric_lakehouse_tables.R
\name{fabric_lakehouse_read_table}
\alias{fabric_lakehouse_read_table}
\title{Read a Microsoft Fabric Lakehouse table}
\usage{
fabric_lakehouse_read_table(
  lakehouse,
  table,
  workspace = NULL,
  schema = NULL,
  columns = NULL,
  limit = NULL,
  version = NULL,
  result = c("tibble", "arrow_stream"),
  verbose = TRUE,
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  auth_args = list(),
  dfs_base = "https://onelake.dfs.fabric.microsoft.com"
)
}
\arguments{
\item{lakehouse}{Lakehouse GUID, exact display name, or one Lakehouse object
returned by \code{\link[=fabric_lakehouses]{fabric_lakehouses()}}. A discovered object is recommended
because it carries its workspace ID and default schema.}

\item{table}{Table name or one row returned by \code{\link[=fabric_lakehouse_tables]{fabric_lakehouse_tables()}}.}

\item{workspace}{Workspace GUID, exact display name, or discovered workspace.
Omit it when \code{lakehouse} is a discovered object containing \code{workspaceId}.}

\item{schema}{Optional schema. A table record supplies its schema when this
argument is omitted.}

\item{columns}{Optional unique column names to project before collection.}

\item{limit}{Optional non-negative maximum number of rows to return.}

\item{version}{Optional non-negative Delta table version for time travel.}

\item{result}{Return a \code{"tibble"} or a disk-backed, single-use
\code{"arrow_stream"}. Release a stream with \code{stream[["release"]]()} when using
'nanoarrow' directly, or close the 'arrow' reader that takes ownership of it}

\item{verbose}{Whether to report authentication and read progress.}

\item{tenant_id}{Entra tenant ID. Defaults to
\code{FABRICQUERYR_TENANT_ID}.}

\item{client_id}{Entra application ID. Defaults to
\code{FABRICQUERYR_CLIENT_ID}, then the Azure CLI application ID.}

\item{token}{Optional access token or audience-aware token-provider function.}

\item{auth_args}{Additional sign-in options passed to
\code{\link[AzureAuth:get_azure_token]{AzureAuth::get_azure_token()}}.}

\item{dfs_base}{OneLake DFS service address. A private or regional endpoint
on a discovered object is preferred when this argument is omitted.}
}
\value{
A tibble, or a disk-backed \code{nanoarrow_array_stream} when
\code{result = "arrow_stream"}. Explicit release deletes its temporary file.
}
\description{
Provides the symmetric read counterpart to \code{\link[=fabric_lakehouse_write_table]{fabric_lakehouse_write_table()}}.
It resolves a discovered Lakehouse object and table record, then delegates to the
authenticated OneLake Delta reader. Use \code{result = "arrow_stream"} to keep a
larger result out of R memory.
}
\details{
Direct reads use the Python runtime described in \code{\link[=fabric_delta_config]{fabric_delta_config()}}.
See \code{\link[=fabric_onelake_read_delta_table]{fabric_onelake_read_delta_table()}} for runtime setup, supported Delta
features, OneLake permissions, and column-type conversion rules.
}
\examples{
\dontrun{
# Discover both the Lakehouse and the table to read
workspace <- fabric_workspaces()[[1L]]
lakehouse <- fabric_lakehouses(workspace)[[1L]]
tables <- fabric_lakehouse_tables(lakehouse)
table <- tables[1L, ]

# Read the discovered table into a tibble
rows <- fabric_lakehouse_read_table(lakehouse, table)

# Count rows in batches when the full table may not fit in R memory
row_count <- local({
  stream <- fabric_lakehouse_read_table(
    lakehouse,
    table,
    result = "arrow_stream"
  )
  on.exit(nanoarrow::nanoarrow_pointer_release(stream), add = TRUE)
  reader <- arrow::as_record_batch_reader(stream)
  on.exit(reader$Close(), add = TRUE, after = FALSE)
  count <- 0
  repeat {
    batch <- reader$read_next_batch()
    if (is.null(batch)) break
    count <- count + batch$num_rows
  }
  count
})
}
}
\references{
\href{https://learn.microsoft.com/en-us/fabric/onelake/table-apis/delta-table-apis-overview}{OneLake table APIs for Delta}

\href{https://learn.microsoft.com/en-us/fabric/onelake/onelake-access-api}{Connect to OneLake}
}
