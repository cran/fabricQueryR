% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fabric_livy_recovery.R
\name{fabric_livy_sessions}
\alias{fabric_livy_sessions}
\alias{fabric_livy_batches}
\alias{fabric_livy_session_attach}
\alias{fabric_livy_batch_attach}
\title{Discover and reattach to Microsoft Fabric Livy work}
\usage{
fabric_livy_sessions(
  livy_url,
  high_concurrency = FALSE,
  top = 100L,
  skip = 0L,
  count = TRUE,
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  auth_args = list(),
  audience = NULL
)

fabric_livy_batches(
  livy_url,
  top = 100L,
  skip = 0L,
  count = TRUE,
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  auth_args = list(),
  audience = NULL
)

fabric_livy_session_attach(
  livy_url,
  session_id,
  high_concurrency = FALSE,
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  auth_args = list(),
  audience = NULL,
  verbose = TRUE
)

fabric_livy_batch_attach(
  livy_url,
  batch_id,
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  auth_args = list(),
  audience = NULL,
  verbose = TRUE
)
}
\arguments{
\item{livy_url}{A copied session or batch connection URL, Livy API base URL,
or enriched Lakehouse object from \code{\link[=fabric_lakehouses]{fabric_lakehouses()}} or \code{\link[=fabric_item]{fabric_item()}}}

\item{high_concurrency}{For \code{fabric_livy_session_attach()}, whether to attach
to a high-concurrency session. \code{fabric_livy_sessions()} only lists regular
sessions because the Livy endpoint does not expose a high-concurrency
collection-list operation}

\item{top}{Maximum records requested for this page}

\item{skip}{Number of matching records to skip}

\item{count}{Whether Fabric should include the total matching record count.
When Fabric returns only a count, the matching page is retrieved separately;
the total and rows can therefore reflect different instants.}

\item{tenant_id}{Microsoft Entra tenant ID. Defaults to
\code{FABRICQUERYR_TENANT_ID}}

\item{client_id}{Microsoft Entra application/client ID. Defaults to
\code{FABRICQUERYR_CLIENT_ID}, then the Azure CLI application ID}

\item{token}{Optional access token or token-provider function. Leave \code{NULL}
to use the normal sign-in flow for a Microsoft Fabric host. A custom
\code{livy_url} requires an explicitly supplied token or provider}

\item{auth_args}{Additional sign-in options passed to
\code{\link[AzureAuth:get_azure_token]{AzureAuth::get_azure_token()}}}

\item{audience}{Optional sign-in scopes. Delegated sign-in defaults to the
required Fabric Livy scopes; client credentials require one \code{.default}
audience}

\item{session_id, batch_id}{Service GUID returned by a list or submit operation}

\item{verbose}{Logical. Show handle lifecycle messages}
}
\value{
\code{fabric_livy_sessions()} and \code{fabric_livy_batches()} return one page
as a tibble with columns \code{id}, \code{name}, \code{state}, \code{result}, \code{app_id}, service
timestamps, and \code{raw}. The tibble has \code{total_count}, \code{page_size}, and
\code{skip} attributes. The attach functions return a \link{FabricLivySession} or
\link{FabricLivyBatch} with a fresh in-process credential.
}
\description{
Lists existing Livy sessions or batches and creates a newly authenticated
handle for work that was started by an earlier R process. Listing never
returns authentication credentials. Attaching retrieves current service
state and does not create a new session or batch.
}
\section{Restart recovery}{

Livy handles intentionally do not serialize their credentials. Store the
service ID, then call the corresponding attach function after restarting R.
Attaching only reconstructs the local handle; it never submits new Spark
work.
}

\section{High-concurrency recovery}{

Fabric supports acquiring an HC session and getting or deleting one by its
HC session ID, but it does not expose a collection \code{GET} for
\code{highConcurrencySessions}. Store the ID returned by
\code{\link[=fabric_livy_session]{fabric_livy_session()}} and pass it to \code{fabric_livy_session_attach()} with
\code{high_concurrency = TRUE}. Calling \code{fabric_livy_sessions()} with
\code{high_concurrency = TRUE} fails locally instead of sending an unsupported
request.
}

\examples{
\dontrun{
workspace <- fabric_workspaces()[[1L]]
lakehouse <- fabric_lakehouses(workspace)[[1L]]

sessions <- fabric_livy_sessions(lakehouse)
session <- fabric_livy_session_attach(lakehouse, sessions$id[[1L]])
session$status()

batches <- fabric_livy_batches(lakehouse)
batch <- fabric_livy_batch_attach(lakehouse, batches$id[[1L]])
batch$status()
}

}
\seealso{
\href{https://github.com/microsoft/fabric-samples/blob/main/docs-samples/data-engineering/Livy-API-swagger/swagger.json}{Microsoft Fabric Livy API specification}
and \href{https://learn.microsoft.com/en-us/fabric/data-engineering/get-started-high-concurrency-livy#api-endpoints-reference}{Microsoft's high-concurrency endpoint reference}
}
