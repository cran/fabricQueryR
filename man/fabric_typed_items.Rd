% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fabric_discovery.R
\name{fabric_typed_items}
\alias{fabric_typed_items}
\alias{fabric_lakehouses}
\alias{fabric_warehouses}
\alias{fabric_warehouse_snapshots}
\alias{fabric_mirrored_databases}
\alias{fabric_sql_databases}
\alias{fabric_semantic_models}
\alias{fabric_eventhouses}
\alias{fabric_kql_databases}
\alias{fabric_notebooks}
\alias{fabric_data_pipelines}
\alias{fabric_spark_job_definitions}
\alias{fabric_environments}
\alias{fabric_graphql_apis}
\title{Typed Microsoft Fabric item discovery}
\usage{
fabric_lakehouses(workspace, detail = TRUE, ...)

fabric_warehouses(workspace, detail = TRUE, ...)

fabric_warehouse_snapshots(workspace, detail = TRUE, ...)

fabric_mirrored_databases(workspace, detail = TRUE, ...)

fabric_sql_databases(workspace, detail = TRUE, ...)

fabric_semantic_models(workspace, detail = FALSE, ...)

fabric_eventhouses(workspace, detail = TRUE, ...)

fabric_kql_databases(workspace, detail = TRUE, ...)

fabric_notebooks(workspace, detail = TRUE, ...)

fabric_data_pipelines(workspace, detail = TRUE, ...)

fabric_spark_job_definitions(workspace, detail = TRUE, ...)

fabric_environments(workspace, detail = TRUE, ...)

fabric_graphql_apis(workspace, detail = FALSE, ...)
}
\arguments{
\item{workspace}{Workspace name, ID, or object returned by
\code{\link[=fabric_workspaces]{fabric_workspaces()}}. A name is convenient for interactive use; an object
avoids an extra lookup}

\item{detail}{Whether to retrieve connection details as well as names and
IDs. This takes more requests and may require additional permissions. For
\code{fabric_item()}, \code{NULL} enriches every supported type except User Data
Functions, whose detail endpoint does not support application identities.
The typed Semantic Model, GraphQL, and User Data Function helpers also
default to lightweight records}

\item{...}{Authentication and API arguments forwarded to \code{\link[=fabric_items]{fabric_items()}}
Do not supply \code{type}; each helper sets that value}
}
\value{
A list with one \link{FabricItem} object or type-specific R6 subclass per
matching item. Each object contains common item metadata, applicable
connection fields, and workload methods. See \code{\link[=fabric_items]{fabric_items()}} for details
}
\description{
These shortcuts cover an intentional subset of Microsoft Fabric item types;
they are not an exhaustive list of the items that \code{\link[=fabric_items]{fabric_items()}} can
discover. Each helper requests one exact type and has a corresponding
\link{FabricWorkspace} method. Most retrieve workload connection details by
default. Semantic Model and GraphQL helpers default to lightweight discovery
because their executable targets are derived from list-level IDs and
workspace fields. User Data Functions default to lightweight discovery
because Microsoft limits detail retrieval to delegated user identities. Set
\code{detail = TRUE} when the workload and identity support it
}
\section{Typed support matrix}{

\verb{Default detail} is the value used when \code{detail} is omitted. \code{FabricItem}
in the final column means that the typed helper and workload Get route are
supported but no workload-specific R6 subclass is currently provided.

\tabular{llll}{
\strong{Helper} \tab \strong{Fabric type} \tab \strong{Default detail} \tab \strong{R6 class} \cr
\code{fabric_lakehouses()} \tab \code{Lakehouse} \tab \code{TRUE} \tab \code{FabricLakehouse} \cr
\code{fabric_warehouses()} \tab \code{Warehouse} \tab \code{TRUE} \tab \code{FabricWarehouse} \cr
\code{fabric_warehouse_snapshots()} \tab \code{WarehouseSnapshot} \tab \code{TRUE} \tab \code{FabricWarehouseSnapshot} \cr
\code{fabric_mirrored_databases()} \tab \code{MirroredDatabase} \tab \code{TRUE} \tab \code{FabricMirroredDatabase} \cr
\code{fabric_sql_databases()} \tab \code{SQLDatabase} \tab \code{TRUE} \tab \code{FabricSqlDatabase} \cr
\code{fabric_semantic_models()} \tab \code{SemanticModel} \tab \code{FALSE} \tab \code{FabricSemanticModel} \cr
\code{fabric_eventhouses()} \tab \code{Eventhouse} \tab \code{TRUE} \tab \code{FabricEventhouse} \cr
\code{fabric_kql_databases()} \tab \code{KQLDatabase} \tab \code{TRUE} \tab \code{FabricKqlDatabase} \cr
\code{fabric_notebooks()} \tab \code{Notebook} \tab \code{TRUE} \tab \code{FabricJobItem} \cr
\code{fabric_data_pipelines()} \tab \code{DataPipeline} \tab \code{TRUE} \tab \code{FabricJobItem} \cr
\code{fabric_spark_job_definitions()} \tab \code{SparkJobDefinition} \tab \code{TRUE} \tab \code{FabricJobItem} \cr
\code{fabric_environments()} \tab \code{Environment} \tab \code{TRUE} \tab \code{FabricItem} \cr
\code{fabric_user_data_functions()} \tab \code{UserDataFunction} \tab \code{FALSE} \tab \code{FabricItem} \cr
\code{fabric_graphql_apis()} \tab \code{GraphQLApi} \tab \code{FALSE} \tab \code{FabricGraphQLApi} \cr
}
}

\section{Choosing a helper}{

\itemize{
\item \code{fabric_lakehouses()}, \code{fabric_warehouses()},
\code{fabric_warehouse_snapshots()}, and \code{fabric_mirrored_databases()} find data
stores with \verb{$sql_query()} (\code{\link[=fabric_sql_query]{fabric_sql_query()}}) and other SQL methods;
Lakehouses and
mirrored databases can also be accessed through OneLake
\item \code{fabric_sql_databases()} finds transactional Fabric SQL databases
\item \code{fabric_semantic_models()} finds business models with \verb{$dax_query()}
(\code{\link[=fabric_pbi_dax_query]{fabric_pbi_dax_query()}}) and refresh lifecycle methods
\item \code{fabric_eventhouses()} and \code{fabric_kql_databases()} find real-time data
stores with \verb{$query()} (\code{\link[=fabric_kql_query]{fabric_kql_query()}}) and \verb{$read_table()}
(\code{\link[=fabric_kql_read_table]{fabric_kql_read_table()}}), plus ingestion and export methods
\item \code{fabric_notebooks()} finds notebooks with job lifecycle and schedule methods
\item \code{fabric_data_pipelines()} and \code{fabric_spark_job_definitions()} find the
other executable items with the same job methods
\item \code{fabric_environments()} finds reusable Spark runtime configurations
\item \code{fabric_user_data_functions()} finds serverless Python function items
\item \code{fabric_graphql_apis()} finds APIs configured in Fabric with \verb{$query()}
(\code{\link[=fabric_graphql_query]{fabric_graphql_query()}}), \verb{$schema()} (\code{\link[=fabric_graphql_schema]{fabric_graphql_schema()}}), and
\verb{$paginate()} (\code{\link[=fabric_graphql_paginate]{fabric_graphql_paginate()}})
}
}

\section{Filtering and returned fields}{

Each helper requests its exact Fabric item type and verifies that every
returned object has that type. The objects otherwise keep all fields
returned by Fabric, including fields added by the service in the future

Folder recursion, workspace-specific private-link routing, authentication,
and \code{detail_errors} have the same behavior as in \code{\link[=fabric_items]{fabric_items()}}. With
\code{detail = TRUE}, each helper calls its documented workload-specific Get API
and preserves fields such as Spark job and Environment properties. The User
Data Function detail endpoint supports delegated users but not service
principals or managed identities; those callers can use \code{detail = FALSE}
}

\examples{
\dontrun{
# Discover a workspace once, then reuse its object for typed discovery
workspace <- fabric_workspaces()[[1]]

# Discover data items that feed the package's query and storage helpers
lakehouses <- fabric_lakehouses(workspace)
warehouses <- fabric_warehouses(workspace)
snapshots <- fabric_warehouse_snapshots(workspace)
mirrored_databases <- fabric_mirrored_databases(workspace)
sql_databases <- fabric_sql_databases(workspace)
semantic_models <- fabric_semantic_models(workspace)
eventhouses <- fabric_eventhouses(workspace)
kql_databases <- fabric_kql_databases(workspace)
graphql_apis <- fabric_graphql_apis(workspace)

# Each method calls the corresponding exported function
# fabric_lakehouse_tables()
lakehouses[[1L]]$tables()
# fabric_sql_connection_info()
warehouses[[1L]]$sql_connection_info()
# fabric_pbi_dax_query()
semantic_models[[1L]]$dax_query(
  dax = Sys.getenv("FABRIC_DAX_QUERY")
)

# Runnable methods call fabric_job_run() and fabric_job_wait()
notebook <- fabric_notebooks(workspace)[[1]]
pipeline <- fabric_data_pipelines(workspace)[[1]]
spark_job <- fabric_spark_job_definitions(workspace)[[1]]

notebook$wait(notebook$run(), timeout = 900)
pipeline$wait(pipeline$run(), timeout = 900)
spark_job$wait(spark_job$run(), timeout = 900)

# Discover supporting Spark and serverless-function items as well
environments <- fabric_environments(workspace)
functions <- fabric_user_data_functions(workspace)
}
}
\references{
\href{https://learn.microsoft.com/en-us/rest/api/fabric/core/items/list-items}{List items REST API}

\href{https://learn.microsoft.com/en-us/rest/api/fabric/datapipeline/items/list-data-pipelines}{List data pipelines}

\href{https://learn.microsoft.com/en-us/rest/api/fabric/sparkjobdefinition/items/list-spark-job-definitions}{List Spark job definitions}

\href{https://learn.microsoft.com/en-us/rest/api/fabric/environment/items/list-environments}{List environments}

\href{https://learn.microsoft.com/en-us/rest/api/fabric/userdatafunction/items/list-user-data-functions}{List User Data Functions}

\href{https://learn.microsoft.com/en-us/rest/api/fabric/datapipeline/items/get-data-pipeline}{Get data pipeline}

\href{https://learn.microsoft.com/en-us/rest/api/fabric/sparkjobdefinition/items/get-spark-job-definition}{Get Spark job definition}

\href{https://learn.microsoft.com/en-us/rest/api/fabric/environment/items/get-environment}{Get environment}

\href{https://learn.microsoft.com/en-us/rest/api/fabric/userdatafunction/items/get-user-data-function}{Get User Data Function}
}
