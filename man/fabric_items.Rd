% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fabric_discovery.R
\name{fabric_items}
\alias{fabric_items}
\title{Discover Microsoft Fabric items}
\usage{
fabric_items(
  workspace,
  type = NULL,
  detail = FALSE,
  detail_errors = c("record", "abort"),
  recursive = TRUE,
  root_folder_id = NULL,
  include = NULL,
  personal_workspace_tenant_id = NULL,
  personal_workspace_owner = NULL,
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  auth_args = list(),
  api_base = .fabric_api_base,
  output = c("r6", "list")
)
}
\arguments{
\item{workspace}{Workspace name, ID, or object returned by
\code{\link[=fabric_workspaces]{fabric_workspaces()}}. A name is convenient for interactive use; an object
avoids an extra lookup}

\item{type}{Optional Fabric API item type, for example \code{"Lakehouse"},
\code{"Warehouse"}, \code{"SemanticModel"}, or \code{"Notebook"}. Matching is done by
Fabric, so use the API spelling. Leave \code{NULL} to list all item types}

\item{detail}{Whether to retrieve connection details as well as names and
IDs. This takes more requests and may require additional permissions. For
\code{fabric_item()}, \code{NULL} enriches every supported type except User Data
Functions, whose detail endpoint does not support application identities.
The typed Semantic Model, GraphQL, and User Data Function helpers also
default to lightweight records}

\item{detail_errors}{What to do if some connection details cannot be read
\code{"record"} returns the available information and stores an error message
with the affected item; \code{"abort"} stops the call}

\item{recursive}{Logical. \code{TRUE} includes items inside workspace folders;
\code{FALSE} lists only items at the workspace root}

\item{root_folder_id}{Optional Fabric folder GUID used as the root of the
listing. With \code{recursive = FALSE}, only direct children are returned;
with \code{TRUE}, nested folders are included}

\item{include}{Optional character vector of additional item properties to
request. Fabric currently documents \code{"DefaultIdentity"}; values are sent
as the API's comma-separated \code{include} query parameter}

\item{personal_workspace_tenant_id}{Optional Microsoft Entra tenant ID used
to build the XMLA endpoint for a Personal workspace}

\item{personal_workspace_owner}{Optional owner UPN or Entra object ID used
to build the XMLA endpoint for a Personal workspace. Microsoft Fabric's
workspace API does not return either personal-workspace identifier, so
supply this together with \code{personal_workspace_tenant_id} when a
\code{dax_connection_string} is needed for a semantic model in My Workspace}

\item{tenant_id}{Microsoft Entra tenant ID. Defaults to
\code{FABRICQUERYR_TENANT_ID}}

\item{client_id}{Microsoft Entra application/client ID. Defaults to
\code{FABRICQUERYR_CLIENT_ID}, then the Azure CLI application ID}

\item{token}{Optional access token or token-provider function. Leave \code{NULL}
to let 'fabricQueryR' use its normal sign-in flow}

\item{auth_args}{Additional sign-in options passed to
\code{\link[AzureAuth:get_azure_token]{AzureAuth::get_azure_token()}}}

\item{api_base}{Fabric REST API base URL. When \code{workspace} is an object
containing \code{apiEndpoint}, that workspace-specific endpoint is used unless
\code{api_base} is supplied explicitly}

\item{output}{Discovery record representation. The default \code{"r6"} returns
R6 objects with type-specific methods. Use \code{"list"} when a plain record is
specifically required}
}
\value{
A list with one item object per match. Every object includes common
fields such as \code{id}, \code{displayName}, \code{type}, and \code{workspaceId}. With
\code{output = "r6"}, results are \link{FabricItem} objects or type-specific
subclasses. With \code{output = "list"}, results are \code{fabric_item} lists. With
\code{detail = TRUE}, both representations include connection details when
Fabric makes them available
}
\description{
Returns the Lakehouses, Warehouses, semantic models, notebooks, and other
items stored in a workspace. Every item type returned by Fabric's core list
API can be represented. Where the package has a specialized R6 subclass,
its methods perform the matching query, connection, file, Spark, or job
operations; other types remain complete generic \link{FabricItem} records
}
\details{
The caller needs at least access to the workspace (the Viewer role is
sufficient for the core list operation). Workload enrichment additionally
requires \code{Item.Read.All}/\code{Item.ReadWrite.All} or the corresponding
workload-specific read scope and access to the item
Personal-workspace semantic models use Microsoft's v2 XMLA endpoint and
require both \code{personal_workspace_tenant_id} and \code{personal_workspace_owner}
}
\section{Generic and typed discovery}{

\code{fabric_items()} and \code{workspace$items()} are the broad, future-compatible
discovery interfaces. Their optional \code{type} filter is passed to Fabric, and
item types without package-specific methods are returned as \link{FabricItem}
objects with all service fields, \verb{$details()}, and \verb{$as_list()}.

The helpers documented in \link{fabric_typed_items} are an intentional
convenience subset of Fabric's larger and evolving item catalog. A typed
helper means that the package knows the item-type spelling and workload Get
route; it does not necessarily mean that the result has its own R6 subclass.
See \link{fabric_typed_items} for the exact support matrix
}

\examples{
\dontrun{
# Start by discovering a workspace instead of copying its ID
workspaces <- fabric_workspaces()
workspace <- workspaces[[1L]]

# `$items()` is the object interface to fabric_items()
items <- workspace$items()
vapply(items, `[[`, character(1), "displayName")

# `$lakehouses()` calls fabric_lakehouses(); `$tables()` calls
# fabric_lakehouse_tables()
lakehouse <- workspace$lakehouses()[[1L]]
lakehouse$tables()
}
}
\references{
\href{https://learn.microsoft.com/en-us/rest/api/fabric/core/items/list-items}{List items REST API}

\href{https://learn.microsoft.com/en-us/rest/api/fabric/articles/item-management/item-management-overview}{Fabric item management overview}

\href{https://learn.microsoft.com/en-us/fabric/enterprise/powerbi/service-premium-connect-tools#connecting-to-a-personal-workspace}{Personal-workspace XMLA endpoints}
}
