% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fabric_operations.R
\name{fabric_operation_status}
\alias{fabric_operation_status}
\alias{fabric_operation_wait}
\alias{fabric_operation_result}
\title{Monitor Microsoft Fabric long-running operations}
\usage{
fabric_operation_status(
  operation,
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  auth_args = list(),
  api_base = .fabric_api_base,
  respect_retry_after = TRUE,
  .sleep = Sys.sleep,
  .now = Sys.time
)

fabric_operation_wait(
  operation,
  poll_interval = NULL,
  timeout = 300,
  error_on_failure = TRUE,
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  auth_args = list(),
  api_base = .fabric_api_base,
  .sleep = Sys.sleep,
  .now = Sys.time
)

fabric_operation_result(
  operation,
  wait = TRUE,
  poll_interval = NULL,
  timeout = 300,
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  auth_args = list(),
  api_base = .fabric_api_base,
  .sleep = Sys.sleep,
  .now = Sys.time
)
}
\arguments{
\item{operation}{A \code{fabric_operation} handle, Fabric operation GUID, or
operation state/result URL returned in a \code{Location} header}

\item{tenant_id}{Entra tenant ID. Defaults to \code{FABRICQUERYR_TENANT_ID}}

\item{client_id}{Entra application ID. Defaults to
\code{FABRICQUERYR_CLIENT_ID}, then the Azure CLI application ID}

\item{token}{Optional access token or token-provider function. Leave \code{NULL}
to let 'fabricQueryR' use its normal sign-in flow. A \code{fabric_operation}
handle reuses its stored credential unless authentication arguments are
supplied explicitly}

\item{auth_args}{Additional sign-in options passed to
\code{\link[AzureAuth:get_azure_token]{AzureAuth::get_azure_token()}} when no token source is supplied}

\item{api_base}{Fabric REST API base URL. Most users should keep the default}

\item{respect_retry_after}{Whether to wait until Fabric's recommended next
status-check time before making the request}

\item{.sleep, .now}{Internal hooks for deterministic tests}

\item{poll_interval}{Minimum seconds between status requests. \code{NULL} honors
Fabric's \code{Retry-After} value and otherwise uses a two-second fallback}

\item{timeout}{Positive maximum total seconds to wait, including status
requests}

\item{error_on_failure}{Whether a failed operation should raise a
\code{fabric_operation_failed} condition. Set to \code{FALSE} to inspect the returned
failed state directly}

\item{wait}{Whether to wait for a running operation. When \code{FALSE}, one state
request is made immediately without honoring a stored future polling hint;
a non-terminal operation raises \code{fabric_operation_not_ready}.}
}
\value{
\code{fabric_operation_status()} and \code{fabric_operation_wait()} return a
\code{fabric_operation_state} record. \code{fabric_operation_result()} returns a
\code{fabric_operation_result} with \code{value}, \code{content_type}, \code{empty}, HTTP and
request identifiers, and the reusable operation handle. JSON results are
decoded as lists, binary results are raw vectors, and empty results have a
\code{NULL} value
}
\description{
Check, wait for, and retrieve the result of a Fabric operation that continues
after its initiating request returns. Pass the operation handle returned by a
'fabricQueryR' function when possible. To resume work later, save the complete
\code{Location} URL returned by Fabric. A bare operation ID can reconstruct only
the core \verb{/operations/\{id\}} route, not workload-scoped routes
}
\section{Typical workflow}{

A package function that starts asynchronous work may return a
\code{fabric_operation} handle. Use \code{fabric_operation_wait()} to wait for it to
finish and \code{fabric_operation_result()} to retrieve its output. Result
retrieval waits by default, so it is enough for the common case

If the R process restarts, save the service-provided \code{location} and pass it
with fresh authentication arguments. A bare ID is sufficient only for core
operations; after success, core operations are completed by reading the
documented \verb{/operations/\{id\}/result} resource
}

\section{Results and failures}{

\code{fabric_operation_status()} preserves Fabric's status, progress, timestamps,
request identifiers, and structured error. Status values added by Fabric in
the future remain inspectable, but \code{fabric_operation_wait()} stops with a
typed error instead of polling an unfamiliar value indefinitely. The
documented \code{Undefined}, \code{NotStarted}, and \code{Running} values remain pending

Some workload APIs, including Lakehouse table loading, expose completion in
their state response and do not provide a separate \verb{/result} resource. For
those operations, \code{fabric_operation_result()} returns the terminal state
payload as its \code{value}

A failed operation raises \code{fabric_operation_failed} by default. A timeout
raises \code{fabric_operation_timeout}; neither condition repeats the request that
originally started the operation
}

\section{Regional operation endpoints}{

Fabric can return a \code{Location} on a regional \verb{*.analysis.windows.net}
cluster. 'fabricQueryR' recognizes those Microsoft endpoints and automatically
uses the Power BI token audience they require. Normal automatic sign-in or an
audience-aware token-provider function handles both audiences. A single
static Fabric bearer token cannot authenticate a regional operation URL
}

\examples{
\dontrun{
# Discover a Lakehouse and a CSV file that Fabric can load as a table
workspace <- fabric_workspaces()[[1L]]
lakehouse <- fabric_lakehouses(workspace)[[1L]]
files <- fabric_onelake_list(
  workspace,
  lakehouse,
  path = "Files/incoming"
)
csv_file <- files[grepl("[.]csv$", files$path), ][1L, ]

# The load call returns the long-running operation handle used below
operation <- fabric_lakehouse_load_table(
  lakehouse,
  table = "orders_imported",
  path = csv_file$path[[1L]],
  format = "Csv",
  header = TRUE
)

# Check once, wait for completion, then retrieve the operation result
state <- fabric_operation_status(operation)
completed <- fabric_operation_wait(state$operation, timeout = 900)
result <- fabric_operation_result(completed$operation)
result$value
}
}
\references{
\href{https://learn.microsoft.com/en-us/rest/api/fabric/core/long-running-operations/get-operation-state}{Get operation state}

\href{https://learn.microsoft.com/en-us/rest/api/fabric/core/long-running-operations/get-operation-result}{Get operation result}

\href{https://learn.microsoft.com/en-us/fabric/real-time-intelligence/map/tutorial-create-fabric-map-python}{Regional Fabric LRO authentication example}
}
