% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fabric_kql_ingestion.R
\name{fabric_kql_export}
\alias{fabric_kql_export}
\title{Export a KQL query directly to external storage}
\usage{
fabric_kql_export(
  cluster,
  query,
  destination,
  database = NULL,
  workspace = NULL,
  path = NULL,
  item_type = NULL,
  format = c("parquet", "csv", "tsv", "json"),
  compressed = TRUE,
  include_headers = NULL,
  name_prefix = NULL,
  file_extension = NULL,
  encoding = NULL,
  compression_type = NULL,
  distribution = c("per_shard", "per_node", "single"),
  size_limit = 1e+08,
  parquet_row_group_size = NULL,
  parquet_datetime_precision = NULL,
  timeout = 900,
  poll_interval = 2,
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  auth_args = list(),
  numeric_policy = c("exact", "service"),
  .sleep = Sys.sleep,
  .now = Sys.time
)
}
\arguments{
\item{cluster}{Query URI, or one Eventhouse or KQLDatabase discovery object.
A KQLDatabase object also supplies \code{database}.}

\item{query}{One non-empty KQL query. The first result set is exported.}

\item{destination}{A discovered Fabric item, item name or ID, complete
OneLake path, or complete HTTPS/ABFSS/ADL connection string for writable
Azure Blob, ADLS Gen1/Gen2, or Amazon S3 storage. Arbitrary HTTPS web
resources are not writable export destinations. A character vector of
complete paths distributes export work across multiple destinations. For
an item, also supply \code{path} and optionally \code{workspace}.}

\item{database}{KQL database display name. Omit for a discovered KQLDatabase.}

\item{workspace}{Workspace containing an item supplied as \code{destination}.
Omit when the discovered item contains its workspace ID.}

\item{path}{Destination directory relative to the OneLake item. It must be
below \verb{Files/}. Omit when \code{destination} is a complete storage path.}

\item{item_type}{Optional Fabric item type used to resolve a named item.}

\item{format}{Storage artifact format.}

\item{compressed}{Whether the artifacts use compression.}

\item{include_headers}{For CSV/TSV, one of \code{"none"}, \code{"all"}, or
\code{"firstFile"}. \code{NULL} uses Kusto's default.}

\item{name_prefix}{Optional prefix for generated artifact names.}

\item{file_extension}{Optional artifact extension beginning with a dot.}

\item{encoding}{For CSV/TSV/JSON text, \code{"UTF8NoBOM"} or \code{"UTF8BOM"}.}

\item{compression_type}{Optional compression codec. Non-Parquet exports use
\code{"gzip"}; Parquet also supports \code{"snappy"}, \code{"lz4_raw"}, \code{"brotli"},
and \code{"zstd"}.}

\item{distribution}{Kusto export distribution hint.}

\item{size_limit}{Maximum uncompressed bytes per artifact, from 100 MB to
4 GB (100,000,000 to 4,000,000,000 bytes).}

\item{parquet_row_group_size}{Optional positive Parquet row-group row count.}

\item{parquet_datetime_precision}{Optional \code{"millisecond"} or
\code{"microsecond"} precision for Parquet datetime values. \code{NULL} omits the
setting and uses Kusto's default of milliseconds, discarding finer
precision. Set \code{"microsecond"} to retain six fractional digits. Neither
setting preserves Kusto's seventh fractional digit (100-nanosecond ticks).
For full source precision, project a text column in the KQL query with
\code{format_datetime(value, 'yyyy-MM-dd HH:mm:ss.fffffff')} before exporting
(Kusto datetimes are UTC).
\code{numeric_policy} governs decimals, not datetime precision.}

\item{timeout}{Positive total client-side limit in seconds, shared by
schema preflight, submission, status polling, and retrieval of artifact
details.}

\item{poll_interval}{Positive seconds between operation status requests.}

\item{tenant_id}{Microsoft Entra tenant ID. Defaults to
\code{FABRICQUERYR_TENANT_ID}}

\item{client_id}{Microsoft Entra application/client ID. Defaults to
\code{FABRICQUERYR_CLIENT_ID}, with the Azure CLI application ID as fallback}

\item{token}{Optional access token or token-provider function. Leave \code{NULL}
to let 'fabricQueryR' use its normal sign-in flow}

\item{auth_args}{Additional sign-in options passed to
\code{\link[AzureAuth:get_azure_token]{AzureAuth::get_azure_token()}}}

\item{numeric_policy}{Decimal Parquet policy. \code{"exact"} refuses queries with
decimal output before export. \code{"service"} explicitly accepts service
conversion, which can change decimal values even on successful exports.
This option does not change other export formats.}

\item{.sleep, .now}{Internal hooks for deterministic polling tests.}
}
\value{
A \code{fabric_kql_export_result} containing the operation state,
redacted destination, artifact paths, per-artifact record counts, and
aggregate record count.
}
\description{
Runs Kusto's server-side \verb{.export to storage} command and waits for its
asynchronous operation to finish. This avoids returning a large query result
through R and the Kusto client-result channel. A discovered Fabric item plus
a \verb{Files/} directory is converted to a OneLake connection string using
caller impersonation; a complete documented Kusto storage connection string
can also be supplied.
}
\section{Tracking and failure safety}{

The export submission is sent once and is never automatically replayed. The
function polls \verb{.show operations} until Kusto reports a terminal state, then
calls \verb{.show operation ... details} for the authoritative artifact paths and
record counts. Kusto does not remove files written before a failed export, so
a failure or timeout identifies the destination and operation ID but never
reports partial files as a successful result.
If Kusto has already reported \code{Completed} but the artifact-details request
exhausts the client deadline, the resulting details-timeout condition records
\code{operation_completed = TRUE}; it does not imply that the export is still
running or failed.

Storage connection strings are emitted as obfuscated Kusto string literals
and are redacted from returned objects and conditions. If a submission fails
before its operation ID is received, inspect the destination and Kusto
operation history before trying again.
}

\section{Output properties}{

\code{format} supports Kusto's \code{parquet}, \code{csv}, \code{tsv}, and \code{json} exporters.
\code{compressed = TRUE} enables the selected \code{compression_type}, or Kusto's
default codec when it is omitted. \code{size_limit} is the uncompressed target
size of each artifact and must be from 100 MB through 4 GB. Text header and
encoding options, and Parquet row-group and datetime-precision options, are
accepted only for their applicable formats.
}

\section{Decimal Parquet safety}{

By default, Parquet exports first request the query's output schemas without
changing its text. Any decimal output raises
\code{fabric_kql_export_decimal_error} before export submission, including an
empty decimal result. Kusto can silently replace large decimals with zero
and truncate fractional digits when exporting to Parquet; \code{Completed} only
confirms the operation completed. Failed or unrecognized schema responses
also stop the export. The schema check does not lock a query's schema against
changes between the check and export requests.

To retain decimal values, explicitly project them as strings in your query,
for example \verb{| project value_text=tostring(value), value_is_null=isnull(value)}.
The companion null flag is necessary because \code{tostring()} turns a null into
an empty string. This preserves Kusto's decimal value text, including any
canonicalization already applied by the service, rather than its original
input precision or scale. Alternatively, explicitly select
\code{numeric_policy = "service"} to accept Kusto's Parquet conversion. The policy
is also forwarded by an Eventhouse or KQLDatabase item's \verb{$export()} method.
Other export formats retain their service-defined conversion behavior.
}

\section{Permissions}{

The caller needs at least Kusto Database Viewer permission. OneLake caller
impersonation additionally needs write access equivalent to Storage Blob
Data Contributor on the destination.
}

\examples{
\dontrun{
# Discover both the source KQL database and destination Lakehouse
workspace <- fabric_workspaces()[[1L]]
database <- fabric_kql_databases(workspace)[[1L]]
lakehouse <- fabric_lakehouses(workspace)[[1L]]
table <- Sys.getenv("FABRIC_KQL_TABLE")
table_literal <- jsonlite::toJSON(table, auto_unbox = TRUE)

# Export a bounded query to a new folder in the discovered Lakehouse
exported <- fabric_kql_export(
  database,
  query = paste0("table(", table_literal, ") | take 10000"),
  destination = lakehouse,
  path = "Files/exports/events-weekly",
  format = "parquet",
  parquet_datetime_precision = "microsecond",
  name_prefix = "events"
)
exported$artifacts
}
}
\references{
\href{https://learn.microsoft.com/en-us/kusto/management/data-export/export-data-to-storage?view=microsoft-fabric}{Kusto export to storage}

\href{https://learn.microsoft.com/en-us/kusto/api/connection-strings/storage-connection-strings?view=microsoft-fabric}{Kusto storage connection strings}

\href{https://learn.microsoft.com/en-us/kusto/api/rest/request?view=microsoft-fabric}{Kusto management HTTP request}

\href{https://learn.microsoft.com/en-us/kusto/management/show-operations?view=microsoft-fabric}{Show Kusto operations}

\href{https://learn.microsoft.com/en-us/kusto/api/rest/request-properties?view=microsoft-fabric}{Kusto request properties}
}
