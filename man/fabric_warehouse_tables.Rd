% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fabric_warehouse_tables.R
\name{fabric_warehouse_tables}
\alias{fabric_warehouse_tables}
\title{Discover Microsoft Fabric Warehouse tables}
\usage{
fabric_warehouse_tables(
  warehouse,
  workspace = NULL,
  schema = NULL,
  detail = TRUE,
  page_size = NULL,
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  auth_args = list(),
  api_base = .fabric_api_base,
  table_api_base = .fabric_onelake_table_base,
  storage_token = NULL
)
}
\arguments{
\item{warehouse}{Warehouse GUID, exact display name, or one Warehouse object
returned by \code{\link[=fabric_warehouses]{fabric_warehouses()}}. A discovered object is recommended
because it contains the workspace and item IDs.}

\item{workspace}{Workspace GUID, exact display name, or discovered workspace.
Omit it when \code{warehouse} is an object containing \code{workspaceId}.}

\item{schema}{Optional Warehouse schema. When omitted, every schema is
listed.}

\item{detail}{Whether to retrieve per-table column metadata.}

\item{page_size}{Optional maximum records requested per OneLake metadata
page, from 1 to 100. All continuation tokens are followed.}

\item{tenant_id}{Entra tenant ID. Defaults to
\code{FABRICQUERYR_TENANT_ID}.}

\item{client_id}{Entra application ID. Defaults to
\code{FABRICQUERYR_CLIENT_ID}, then the Azure CLI application ID.}

\item{token}{Optional access token or audience-aware token-provider function.
Warehouse lookup can require a Fabric-audience token; table metadata uses
a Storage-audience token.}

\item{auth_args}{Additional sign-in options passed to
\code{\link[AzureAuth:get_azure_token]{AzureAuth::get_azure_token()}} when no token source is supplied.}

\item{api_base}{Fabric REST API base URL used when a Warehouse name or GUID
must be resolved. Most users should keep the default.}

\item{table_api_base}{OneLake Delta table API base URL. Most users should
keep the default.}

\item{storage_token}{Optional separate Azure Storage token or token-provider
function. Supply it when \code{token} is fixed and Warehouse lookup is needed.}
}
\value{
A tibble with table \code{name}, \code{schema}, \code{full_name}, \code{type}, \code{format},
\code{location}, timestamps, list-column \code{columns}, \code{schema_metadata}, and the
unmodified OneLake \code{raw} record. \code{fabric_raw} is an empty list-column
because Fabric does not expose a Warehouse counterpart to the Lakehouse
List Tables REST route. Unknown future OneLake metadata remains available
in \code{raw}.
}
\description{
Lists schemas and Delta-backed tables in a Fabric Warehouse through the
read-only OneLake table metadata API. Set \code{detail = TRUE} to retrieve column
metadata for every table. A returned row can be passed directly to
\code{\link[=fabric_warehouse_read_table]{fabric_warehouse_read_table()}}.
}
\section{Permissions}{

The OneLake table API uses the Azure Storage token audience and requires the
calling identity to have permission to read tables in the Warehouse through
OneLake. This permission is separate from Warehouse T-SQL \code{ReadData}
permission.
}

\examples{
\dontrun{
workspace <- fabric_workspaces()[[1L]]
warehouse <- fabric_warehouses(workspace)[[1L]]

tables <- fabric_warehouse_tables(warehouse)
orders <- fabric_warehouse_read_table(warehouse, tables[1L, ])
}
}
\references{
\href{https://learn.microsoft.com/en-us/rest/api/fabric/articles/onelakecatalog/overview#explore-tables-within-an-item}{Explore tables with OneLake catalog APIs}

\href{https://learn.microsoft.com/en-us/fabric/onelake/table-apis/delta-table-apis-overview}{OneLake table APIs for Delta}

\href{https://learn.microsoft.com/en-us/fabric/data-warehouse/share-warehouse-manage-permissions}{Warehouse permissions}
}
