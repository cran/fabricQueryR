% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fabric_jobs.R
\name{fabric_job_run}
\alias{fabric_job_run}
\alias{fabric_job_status}
\alias{fabric_job_wait}
\alias{fabric_job_cancel}
\title{Run and monitor Microsoft Fabric item jobs}
\usage{
fabric_job_run(
  item,
  workspace = NULL,
  job_type = NULL,
  item_type = NULL,
  parameters = NULL,
  parameter_types = NULL,
  execution_data = NULL,
  default_lakehouse = NULL,
  default_lakehouse_workspace = NULL,
  compute = NULL,
  session_tag = NULL,
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  auth_args = list(),
  api_base = .fabric_api_base,
  .sleep = Sys.sleep,
  .now = Sys.time
)

fabric_job_status(
  job = NULL,
  workspace = NULL,
  item = NULL,
  job_instance_id = NULL,
  item_type = NULL,
  job_type = NULL,
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  auth_args = list(),
  api_base = .fabric_api_base,
  respect_retry_after = TRUE,
  notebook_details = FALSE,
  .sleep = Sys.sleep,
  .now = Sys.time
)

fabric_job_wait(
  job,
  poll_interval = NULL,
  timeout = 600,
  error_on_failure = TRUE,
  cancel_on_timeout = FALSE,
  cancel = NULL,
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  auth_args = list(),
  api_base = .fabric_api_base,
  notebook_details = FALSE,
  .sleep = Sys.sleep,
  .now = Sys.time
)

fabric_job_cancel(
  job = NULL,
  workspace = NULL,
  item = NULL,
  job_instance_id = NULL,
  item_type = NULL,
  job_type = NULL,
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  auth_args = list(),
  api_base = .fabric_api_base
)
}
\arguments{
\item{item}{Item GUID, exact display name, or an item object returned
by a discovery function. A discovered object is recommended because it
already includes the item type and workspace ID}

\item{workspace}{Workspace GUID, exact display name, or a discovered object
Omit it when \code{item} is a discovered object containing \code{workspaceId}}

\item{job_type}{Fabric job type. 'fabricQueryR' uses the current typed
\code{"Execute"} operation for data pipelines and knows the usual values for
notebooks and Spark job definitions, so normally omit this unless running
another item type. Set \code{job_type = "Pipeline"} for a data pipeline only when
explicitly using Fabric's legacy core job endpoint}

\item{item_type}{Optional Fabric item type when \code{item} is a GUID. A discovered
item supplies this automatically. Examples are \code{"Notebook"},
\code{"DataPipeline"}, and \code{"SparkJobDefinition"}}

\item{parameters}{A named list of values to pass to the job, such as
\code{list(run_date = as.Date("2026-01-31"), full_load = FALSE)}, infers types
from R and is appropriate for most runs. Names must match the parameters
configured in Fabric. Advanced callers can instead supply records with
\code{name}, \code{value}, and \code{type}. The typed DataPipeline \code{Execute} endpoint does
not accept parameters. \code{bit64::integer64} values infer \code{Number} and are
accepted only when exactly representable as a double, since Fabric may
interpret numeric parameters as floating point. This check also applies
to explicit \code{Number} and \code{Automatic} types, which also require finite
numeric values. Fabric's decimal parameter binder has a narrower range and
scale than R doubles. Numeric parameters are rejected if parsing their JSON
token through that binder would overflow or change the original double.
For other exact integers or decimals, pass character values with type
\code{Text}; for doubles, \code{sprintf("\%.17g", value)} supplies reversible text.
The receiving job must handle these values as text.
Fabric normalizes numeric negative zero to zero;
pass \code{"-0.0"} with type \code{Text} when its sign must be retained.
R date-times must have whole-second precision. Fractional seconds are
rejected because Fabric's \code{DateTime} format cannot preserve them; round or
truncate explicitly before submission if that loss is acceptable.}

\item{parameter_types}{Optional named character vector overriding inferred
parameter types. Supported values are \code{VariableReference}, \code{Integer},
\code{Number}, \code{Text}, \code{Boolean}, \code{DateTime}, \code{Guid}, and \code{Automatic}. Use this
only when R's inferred type is not the type expected in Fabric}

\item{execution_data}{Optional advanced job settings in the format documented
for the Fabric item type. Use the simpler arguments below for common
notebook settings. In custom payload fields, wrap a one-element atomic
vector in \code{\link[=I]{I()}} (or use an unnamed list) when it must remain a JSON array.
The typed DataPipeline \code{Execute} endpoint does not accept a request body}

\item{default_lakehouse}{Optional Lakehouse GUID or discovered object used to
set the notebook's default Lakehouse for this run. This changes the run
context, not the notebook's saved default}

\item{default_lakehouse_workspace}{Optional workspace GUID or discovered
record for \code{default_lakehouse}. When omitted, a discovered Lakehouse's
workspace is used when available, otherwise the job workspace. An explicit
workspace must match the workspace carried by a discovered Lakehouse}

\item{compute}{Notebook compute kind: \code{"Spark"}, \code{"Jupyter"}, or
\code{"DataWarehouse"}. Use \code{"Spark"} (the default) for Spark notebooks,
\code{"Jupyter"} for a Jupyter runtime, and \code{"DataWarehouse"} for a notebook
attached to Warehouse compute. It must match what the notebook code needs}

\item{session_tag}{Optional tag that enables Spark high-concurrency mode, so
related notebook runs may reuse compute. See Details for its effect on
failure reporting}

\item{tenant_id}{Entra tenant ID. Defaults to
\code{FABRICQUERYR_TENANT_ID}}

\item{client_id}{Entra application ID. Defaults to
\code{FABRICQUERYR_CLIENT_ID}, then the Azure CLI application ID}

\item{token}{Optional access token or token-provider function. Leave \code{NULL}
to let 'fabricQueryR' use its normal sign-in flow
A \code{fabric_job} handle reuses its stored credential unless \code{tenant_id},
\code{client_id}, \code{token}, or non-empty \code{auth_args} is supplied explicitly}

\item{auth_args}{Additional sign-in options passed to
\code{\link[AzureAuth:get_azure_token]{AzureAuth::get_azure_token()}} when no token source is supplied}

\item{api_base}{Fabric REST API base URL. Most users should keep the default
A discovered workspace-specific endpoint is used unless this argument is
supplied explicitly}

\item{.sleep, .now}{Internal hooks for deterministic tests}

\item{job}{A \code{fabric_job} returned by \code{\link[=fabric_job_run]{fabric_job_run()}} or a
\code{fabric_job_instance} returned by a status, wait, or history function.
Status and cancellation functions also accept a job instance GUID when
\code{workspace}, \code{item}, and enough type information are supplied}

\item{job_instance_id}{Alternative argument for a job instance GUID. Do not
supply it together with a handle, instance record, or GUID through \code{job}}

\item{respect_retry_after}{Whether to wait for Fabric's recommended first
status-check time. Keep \code{TRUE} for normal use}

\item{notebook_details}{For Notebook jobs, whether to opt into the beta
Notebook status endpoint for exit values and compute details. The default
\code{FALSE} uses only the stable Core Job Scheduler status endpoint;
\code{exit_value} and workload-specific \code{properties} may then be unavailable.}

\item{poll_interval}{Minimum seconds between status checks. \code{NULL} follows
Fabric's recommendation, with a two-second fallback}

\item{timeout}{Maximum seconds to wait before raising a
\code{fabric_job_timeout}}

\item{error_on_failure}{Whether failed, cancelled, or deduplicated jobs raise
typed errors. Set to \code{FALSE} to inspect those terminal results directly}

\item{cancel_on_timeout}{Ask Fabric to cancel the job when the client-side
timeout expires. \code{FALSE} stops waiting but leaves the Fabric job running}

\item{cancel}{Optional function checked between status updates. If it returns
\code{TRUE}, 'fabricQueryR' requests cancellation. This can support an
application's stop button}
}
\value{
\code{fabric_job_run()} returns a \code{fabric_job} handle for use with the
other job functions
\code{fabric_job_status()} and \code{fabric_job_wait()} return a
\code{fabric_job_instance} record with status, times, failure information, and a
notebook exit value when available. \code{fabric_job_cancel()} invisibly returns
\code{TRUE} after Fabric accepts or confirms the cancellation
}
\description{
Start a Notebook, data pipeline, Spark job definition, or another supported
Fabric item from R. The related functions check its progress, wait for it to
finish, or request cancellation. Use Fabric's scheduler for recurring runs
}
\section{Typical workflow}{

Start a job with \code{fabric_job_run()}, then pass the returned handle to
\code{fabric_job_wait()}. The handle keeps the workspace, item, job type, and
sign-in context, so later calls do not need those details again
Parameterized Core jobs can return a collection \code{Location} without an
instance GUID. In that documented case, \code{fabric_job_run()} honors
\code{Retry-After} and polls recent job history for one matching manual run. If
the accepted instance cannot be resolved safely, it raises a
\code{fabric_job_accepted_unresolved} condition rather than implying that the run
request failed or replaying it
}

\section{High-concurrency notebooks}{

A \code{session_tag} lets related notebook runs share Spark compute, but Fabric may
report a failed statement as a completed shared session with no exit value
Omit the tag when job status must reliably signal notebook failure. Otherwise,
have the notebook report its outcome with \code{notebookutils.notebook.exit()}.
The former \code{mssparkutils} namespace remains backward compatible but Microsoft
recommends migrating because it will be retired

Notebook submission uses the released workload-specific route so Fabric
applies per-run parameters and compute settings. Status and waiting use the
stable Core endpoint by default. Set
\code{notebook_details = TRUE} to opt into the beta Notebook status endpoint when
exit values or workload-specific properties are required; the Core endpoint
remains its fallback.
}

\section{Permissions and status handling}{

Running and cancelling need an item execute permission. Checking or waiting
also needs an item read permission, as does resolving a parameterized run's
collection \code{Location}. For a parameterized Notebook, 'fabricQueryR' captures
recent history before submission so a collection \code{Location} cannot be
confused with an earlier run. Recovery requires response correlation with
the job's root activity ID; history alone cannot establish ownership.
Recovery stops with an accepted-but-unresolved error if correlation is
absent or ambiguous. 'fabricQueryR'
reconciles notebook status information from Fabric before returning it and
stops with a typed error if Fabric reports an unfamiliar state instead of
waiting indefinitely
}

\examples{
\dontrun{
# Discover the workspace and Notebook that will be run
workspace <- fabric_workspaces()[[1L]]
notebook <- fabric_notebooks(workspace)[[1L]]

# Start the discovered Notebook and keep the returned job handle
job <- fabric_job_run(notebook)

# Refresh the current state without waiting for completion
current <- fabric_job_status(job)
current$status

# Opt into beta Notebook details only when an exit value is required
completed <- fabric_job_wait(
  job,
  timeout = 900,
  notebook_details = TRUE
)
completed$status
completed$exit_value

# A separate active run can be cancelled when it is no longer needed
job_to_cancel <- fabric_job_run(notebook)
fabric_job_cancel(job_to_cancel)
}
}
\references{
\href{https://learn.microsoft.com/en-us/rest/api/fabric/core/job-scheduler/}{Core Job Scheduler REST API}

\href{https://learn.microsoft.com/en-us/rest/api/fabric/core/job-scheduler/run-on-demand-item-job}{Run an on-demand item job}

\href{https://learn.microsoft.com/en-us/rest/api/fabric/notebook/background-jobs/run-on-demand-notebook}{Run an on-demand notebook}

\href{https://learn.microsoft.com/en-us/rest/api/fabric/notebook/background-jobs/get-notebook-job-instance\%28beta\%29}{Get a Notebook job instance (beta)}

\href{https://learn.microsoft.com/en-us/fabric/data-engineering/notebook-public-api}{Manage and execute notebooks with public APIs}

\href{https://learn.microsoft.com/en-us/fabric/fundamentals/job-scheduler}{Fabric job scheduler}
}
