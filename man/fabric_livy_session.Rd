% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fabric_livy_session.R
\name{fabric_livy_session}
\alias{fabric_livy_session}
\title{Create a Microsoft Fabric Livy session}
\usage{
fabric_livy_session(
  livy_url,
  high_concurrency = FALSE,
  session_tag = NULL,
  name = NULL,
  tags = NULL,
  conf = NULL,
  environment_id = NULL,
  archives = NULL,
  driver_memory = NULL,
  driver_cores = NULL,
  executor_memory = NULL,
  executor_cores = NULL,
  num_executors = NULL,
  artifact_name = NULL,
  file = NULL,
  class_name = NULL,
  args = NULL,
  jars = NULL,
  files = NULL,
  py_files = NULL,
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  auth_args = list(),
  audience = NULL,
  verbose = TRUE
)
}
\arguments{
\item{livy_url}{A copied session or batch connection URL, Livy API base URL,
or enriched Lakehouse object from \code{\link[=fabric_lakehouses]{fabric_lakehouses()}} or \code{\link[=fabric_item]{fabric_item()}}
Copy the session-job URL from \strong{Lakehouse settings > Livy endpoint}, or
use a discovered object to avoid handling IDs manually}

\item{high_concurrency}{Whether to let Fabric share Spark compute between
several isolated workloads. Keep \code{FALSE} for a typical sequence of calls in
one R process}

\item{session_tag}{Optional high-concurrency packing hint. Related requests
with the same tag may share an underlying Livy session while keeping
separate REPL state. Each call still returns a distinct HC session}

\item{name}{Optional readable session name shown in service metadata}

\item{tags}{Optional named list of string labels for monitoring}

\item{conf}{Optional named list of Spark settings. Prefer a published Fabric
Environment for configuration shared by several jobs}

\item{environment_id}{Optional GUID of a published Fabric Environment whose
libraries and Spark settings should be used}

\item{archives}{Optional character vector of archive URIs made available to
Spark}

\item{driver_memory, executor_memory}{Optional Spark memory values such as
\code{"4g"}. Leave \code{NULL} to use Fabric defaults}

\item{driver_cores, executor_cores, num_executors}{Optional Spark resource
counts. Larger values consume more capacity; leave \code{NULL} unless the
workload has been sized deliberately}

\item{artifact_name}{Optional Lakehouse/artifact label used for a
high-concurrency job in the Fabric Monitoring hub}

\item{file}{Optional application file URI for a high-concurrency request}

\item{class_name}{Optional Java/Scala main class for \code{file}}

\item{args}{Optional character vector of application arguments}

\item{jars, files, py_files}{Optional character vectors of dependency URIs
supplied to Spark}

\item{tenant_id}{Microsoft Entra tenant ID. Defaults to
\code{FABRICQUERYR_TENANT_ID}}

\item{client_id}{Microsoft Entra application/client ID. Defaults to
\code{FABRICQUERYR_CLIENT_ID}, then the Azure CLI application ID}

\item{token}{Optional access token or token-provider function. Leave \code{NULL}
to let 'fabricQueryR' use its normal sign-in flow for a Microsoft Fabric
host. A custom \code{livy_url} requires an explicitly supplied token or provider.
HTTPS validation does not prove ownership or token audience; use a custom
host only when your organization controls it, with a credential issued for
its intended audience}

\item{auth_args}{Additional sign-in options passed to
\code{\link[AzureAuth:get_azure_token]{AzureAuth::get_azure_token()}}}

\item{audience}{Optional sign-in scopes. For delegated sign-in, \code{NULL}
requests the four required Livy scopes listed below. An explicit vector
replaces those defaults, so include every required scope plus any optional
\verb{Code.Access*} scope the Spark code needs. Client credentials require one
\code{.default} audience}

\item{verbose}{Logical. Show session lifecycle messages}
}
\value{
A newly created \link{FabricLivySession}. It may still be starting; call
\verb{$wait()} before \verb{$submit()}/\verb{$run()}, and \verb{$close()} when finished. These
handle lifecycle methods do not have separate free-function wrappers
}
\description{
Starts Spark compute that can run several statements while keeping variables
and Spark state between calls. Use \code{\link[=fabric_livy_query]{fabric_livy_query()}} instead for a single,
self-contained operation
}
\section{Choosing a session type}{

Use a standard session for a typical sequence in one R process. High
concurrency is for applications that run several independent Spark workloads
at the same time; it is not needed for several sequential statements
}

\section{Cleanup and permissions}{

No network request is made when an open object is garbage collected. Call
\verb{$close()} explicitly, and use \code{on.exit(session$close())} inside functions
Delegated sign-in requires \code{Lakehouse.Execute.All}, \code{Lakehouse.Read.All},
\code{Code.AccessFabric.All}, and \code{Code.AccessStorage.All}. Add
\code{Code.AccessAzureKeyvault.All}, \code{Code.AccessAzureDataLake.All},
\code{Code.AccessAzureDataExplorer.All}, or \code{Code.AccessSQL.All} only when Spark
accesses that Azure service at runtime. Microsoft's current session guide
documents delegated-user and service-principal (SPN) tokens. The signed-in
identity also needs an appropriate workspace role, and service-side tenant
settings still apply
}

\section{Timeouts}{

A \code{fabric_livy_timeout_error} contains the exact session or statement object
in its \code{handle} field, so it can be polled or cancelled in the current R
process. The kind-specific \code{session} or \code{statement} field contains safe,
serializable metadata; a serialized handle intentionally loses its
in-process credential
}

\examples{
\dontrun{
# Discover the Lakehouse whose Livy endpoint will host the Spark session
workspace <- fabric_workspaces()[[1L]]
lakehouse <- fabric_lakehouses(workspace)[[1L]]

run_shared_state <- function(lakehouse) {
  # Keep one session alive so successive statements share Spark state
  session <- fabric_livy_session(lakehouse)
  on.exit(session$close(), add = TRUE)
  session$wait()
  session$run("shared_value = 40", kind = "pyspark")
  session$run("print(shared_value + 2)", kind = "pyspark")
}
run_shared_state(lakehouse)

run_high_concurrency <- function(lakehouse) {
  # A session tag lets compatible callers reuse high-concurrency compute
  session <- fabric_livy_session(
    lakehouse,
    high_concurrency = TRUE,
    session_tag = "report-workers"
  )
  on.exit(session$close(), add = TRUE)
  session$wait()
  session$run("SELECT current_timestamp()", kind = "sql")
}
run_high_concurrency(lakehouse)
}

}
\seealso{
\href{https://learn.microsoft.com/en-us/fabric/data-engineering/get-started-api-livy-session}{Microsoft session jobs},
\href{https://learn.microsoft.com/en-us/fabric/data-engineering/high-concurrency-livy}{high-concurrency Livy},
and the \href{https://livy.apache.org/docs/latest/rest-api.html}{Apache Livy REST API}
}
