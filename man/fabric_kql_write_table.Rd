% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fabric_kql_ingestion.R
\name{fabric_kql_write_table}
\alias{fabric_kql_write_table}
\title{Write an R or Arrow object to an Eventhouse table}
\usage{
fabric_kql_write_table(
  cluster,
  table,
  data,
  database = NULL,
  mapping = NULL,
  staging_folder = NULL,
  staging_root = "fabricqueryr-staging",
  cleanup = TRUE,
  keep_staging_on_failure = TRUE,
  compression = "snappy",
  target_file_size = 512 * 1024^2,
  max_rows_per_file = NULL,
  tags = character(),
  ingest_if_not_exists = character(),
  skip_batching = FALSE,
  creation_time = NULL,
  timeout = 900,
  poll_interval = 2,
  error_on_failure = TRUE,
  tenant_id = Sys.getenv("FABRICQUERYR_TENANT_ID"),
  client_id = Sys.getenv("FABRICQUERYR_CLIENT_ID", unset =
    "04b07795-8ddb-461a-bbee-02f9e1bf7b46"),
  token = NULL,
  storage_token = NULL,
  auth_args = list(),
  create_if_missing = FALSE,
  column_types = NULL,
  query_cluster = NULL,
  numeric_policy = c("exact", "service"),
  .sleep = Sys.sleep,
  .now = Sys.time
)
}
\arguments{
\item{cluster}{Ingestion URI or Eventhouse/KQLDatabase discovery object; see
\code{\link[=fabric_kql_ingest]{fabric_kql_ingest()}}.}

\item{table}{Target KQL table name.}

\item{data}{Data frame, tibble, Arrow Table/RecordBatch, lazy Arrow
Dataset/Scanner/query, Arrow RecordBatchReader, or compatible array stream.}

\item{database}{Target KQL database name. Omit for a discovered KQLDatabase.}

\item{mapping}{Optional predefined Parquet ingestion mapping name.}

\item{staging_folder}{Optional trusted OneLake folder URI beginning below an
item's \verb{Files/} area. The ingestion configuration's lake folder is used by
default.}

\item{staging_root}{Relative directory created below the selected lake
folder for package staging.}

\item{cleanup}{Remove OneLake staging after confirmed success, or authorize
Kusto to delete Storage blobs after download.}

\item{keep_staging_on_failure}{Retain staging after a confirmed terminal
Kusto failure. The client never deletes staging after ambiguous failures;
Storage may already have deleted downloaded blobs when \code{cleanup = TRUE}.}

\item{compression}{Parquet compression supported by \code{\link[arrow:write_parquet]{arrow::write_parquet()}}.}

\item{target_file_size}{Soft maximum bytes per staged Parquet file. The
service's advertised total-size and blob-count limits are still enforced.
Storage-container staging uses block upload when a completed file exceeds
Azure Storage's single-request Put Blob limit.}

\item{max_rows_per_file}{Optional exact maximum rows per staged file.}

\item{tags}{Extent tags passed to \code{\link[=fabric_kql_ingest]{fabric_kql_ingest()}}.}

\item{ingest_if_not_exists}{Stable idempotency keys passed to
\code{\link[=fabric_kql_ingest]{fabric_kql_ingest()}}. Requires staging to produce exactly one file.}

\item{skip_batching}{Whether Kusto should bypass normal ingestion batching.}

\item{creation_time}{Optional extent creation time passed to
\code{\link[=fabric_kql_ingest]{fabric_kql_ingest()}}.}

\item{timeout}{Positive number of seconds shared by submission and tracked
status waiting after upload. Time spent submitting reduces the time
available for polling.}

\item{poll_interval}{Minimum seconds between ingestion status requests.}

\item{error_on_failure}{Raise a typed error for a confirmed failed or
canceled ingestion. Set \code{FALSE} to return the failed result and its staging
disposition.}

\item{tenant_id}{Microsoft Entra tenant ID.}

\item{client_id}{Microsoft Entra application/client ID.}

\item{token}{Optional access token or audience-aware token-provider function.
A fixed token must target Kusto and be paired with \code{storage_token}.}

\item{storage_token}{Optional separate Azure Storage access token or token
provider. Required when \code{token} cannot acquire a different audience.}

\item{auth_args}{Additional options passed to \code{\link[AzureAuth:get_azure_token]{AzureAuth::get_azure_token()}}.}

\item{create_if_missing}{Whether to create a missing KQL table from the
Arrow schema after local validation and before upload. Existing tables are
left unchanged.}

\item{column_types}{Optional named character vector giving one Kusto scalar
type for every data column when \code{create_if_missing = TRUE}. Supported
canonical types are \code{bool}, \code{datetime}, \code{decimal}, \code{dynamic}, \code{guid},
\code{int}, \code{long}, \code{real}, and \code{string}. \code{NULL} infers them. Arrow time and
duration columns must be converted because Kusto's Parquet mapping cannot
ingest them as \code{timespan}.}

\item{query_cluster}{Optional Kusto query-service URI or discovery object
used for table creation and identity-schema validation. A discovered
\code{cluster} already carries this URI; a standard Microsoft ingestion URI is
converted to its paired query URI. Supply this explicitly for a trusted
custom ingestion endpoint.}

\item{numeric_policy}{Decimal ingestion policy. \code{"exact"} checks actual
staged decimal values before creation/upload and rejects coefficients
requiring more than 34 significant digits. \code{"service"} explicitly delegates
conversion to Kusto, including possible rounding or replacement by null.
Neither policy changes the source Parquet values or preserves their
precision, scale or trailing-zero spelling in Kusto.}

\item{.sleep, .now}{Internal deterministic polling hooks.}
}
\value{
A \code{fabric_kql_write_result} containing row/file counts, compressed
Parquet \code{bytes}/\code{part_bytes}, diagnostic Arrow
\code{buffer_bytes}/\code{part_buffer_bytes}, normalized ingestion status, tracking
handle, source IDs, and staging disposition.
}
\description{
Serializes an R or Arrow object to Parquet, uploads it using the storage
container or OneLake folder preferred by the Kusto ingestion service,
submits tracked queued ingestion, waits for the terminal per-file result,
and manages staging cleanup. With \code{cleanup = TRUE}, Storage sources may be
deleted after download, before ingestion succeeds; OneLake staging is removed
only after confirmed success. Use \code{cleanup = FALSE} to retain Storage sources
for recovery.
}
\section{One-call staging workflow}{

The queued-ingestion REST API accepts storage blobs rather than inline R
values. This function provides the higher-level one-call workflow: it reads
the ingestion service's preview configuration, honors its preferred upload
method, creates a unique \code{fabricqueryr-staging} path, and uploads bounded
Parquet parts. Service-provided Storage containers use their short-lived SAS
credentials. OneLake staging uses a Storage-audience access token, so an
audience-aware credential obtains both required tokens. When \code{token} is a
fixed bearer token or \code{AzureToken} and OneLake is selected, supply the
separate \code{storage_token}. \code{staging_folder} explicitly selects OneLake and
overrides the advertised upload preference with a trusted \verb{Files/} URI.

The caller therefore needs Kusto Table Ingestor and Database User access,
plus write/delete access when OneLake is selected. Advertised Storage
containers carry the service-managed SAS access needed for staging.
}

\section{R and Arrow inputs}{

Data frames and tibbles are converted through Arrow. Factors become strings;
complex and \code{difftime} columns require an explicit conversion. Arrow Tables,
RecordBatches, Datasets, Scanners, \code{arrow_dplyr_query} objects, and
RecordBatchReaders are accepted, as are Arrow-compatible
\code{nanoarrow_array_stream} objects returned by package query helpers. Lazy
inputs are read one record batch at a time and written directly to a
temporary Parquet parts, so the complete data set is never collected into R
memory. A supplied reader or stream is single-use and is consumed.

Parquet identity mapping matches source fields to existing KQL columns by
case-sensitive name. Before staging, the writer verifies that those names
and their Kusto scalar types exactly match the target table. Supply \code{mapping}
when the Parquet schema and table need an explicit predefined mapping; a
named mapping bypasses this identity-schema check.

\code{ingest_if_not_exists} requires staging to produce one Parquet file,
regardless of \code{skip_batching}. A shared idempotency tag can suppress later
files in the same logical write. Stage one file or omit the idempotency key.

The service's advertised \code{maxDataSize} and source \code{rawSize} refer to the
uncompressed source representation. Arrow's in-memory buffer size is not an
equivalent Parquet measurement, so the writer deliberately omits \code{rawSize}
and lets Kusto inspect the staged Parquet metadata. Compressed file sizes and
Arrow buffer sizes remain available separately in the result.

Set \code{create_if_missing = TRUE} to issue Kusto's idempotent \verb{.create table}
command after local validation and before upload. A missing table is created
from the Arrow schema; an existing table is returned unchanged, so this option
never alters an existing schema. Common Arrow scalar and nested types are
inferred as Kusto types. Supply a named \code{column_types} vector to override every
column type.

By default, decimal values are checked in every staged Parquet batch before
table creation or upload. Kusto ingestion can replace decimals with more than
34 significant digits by null even when ingestion succeeds. Precision above
34 in an Arrow schema is allowed when the actual values fit; insignificant
leading and trailing zeros do not count. The same check applies inside nested
data and with explicit \code{column_types} or a named \code{mapping}. It does not certify
arbitrary transformations in those user-selected mappings. Convert decimal
columns explicitly to Arrow strings to transfer their full text, or select
\code{numeric_policy = "service"} to accept service conversion, rounding and nulls.
Kusto strings merge missing and empty values; preserve a separate null flag
when that distinction matters. The check protects mathematical decimal
values within the staged Parquet representation; Kusto can canonicalize
their precision, scale and trailing-zero spelling.

Service-owned Storage credentials are reacquired after local serialization.
During a multipart upload, the writer honors the advertised configuration
refresh interval and retries once with new credentials when Storage reports
an expired authorization.
}

\section{Failure and cleanup safety}{

A successful tracked ingestion is cleaned up by default. Kusto removes
service-owned Storage blobs after download; the client removes OneLake
staging after confirmed success. Ambiguous results retain OneLake staging.
With Storage and \code{cleanup = TRUE}, an ambiguous or failed batch reports
\code{staging_retained = NA}: some or all blobs may already have been deleted.
Set \code{cleanup = FALSE} to retain Storage sources for recovery.
After a confirmed terminal failure, the client leaves remaining staging
alone unless \code{keep_staging_on_failure = FALSE}. The full staging path is carried
by \code{fabric_kql_write_error} conditions.
A transport failure during OneLake's final atomic rename can also leave the
unique destination present; upload errors report \code{staging_retained = NA} and
the path to inspect.
}

\examples{
\dontrun{
# Discover the KQL database that will receive the R data
workspace <- fabric_workspaces()[[1L]]
database <- fabric_kql_databases(workspace)[[1L]]

# Create a new table when needed, stage the data, and wait for ingestion
result <- fabric_kql_write_table(
  database,
  table = "EventsFromR",
  data = data.frame(id = 1:3, value = c("a", "b", "c")),
  create_if_missing = TRUE,
  ingest_if_not_exists = "r-batch-2026-08-14"
)
result$status$state

# A local Arrow Dataset is scanned batch by batch rather than collected
dataset <- arrow::open_dataset(Sys.getenv("ARROW_DATASET_PATH"))
fabric_kql_write_table(database, "EventsFromArrow", dataset)
}
}
\references{
\href{https://learn.microsoft.com/en-us/kusto/management/data-ingestion/queued-ingest-configuration-http?view=microsoft-fabric}{Queued ingestion configuration REST API (preview)}

\href{https://learn.microsoft.com/en-us/kusto/management/data-ingestion/queued-ingest-use-http?view=microsoft-fabric}{Queued ingestion REST API (preview)}

\href{https://learn.microsoft.com/en-us/kusto/management/create-table-command?view=microsoft-fabric}{Create a Kusto table}

\href{https://learn.microsoft.com/en-us/kusto/query/scalar-data-types/?view=microsoft-fabric}{Kusto scalar data types}

\href{https://learn.microsoft.com/en-us/kusto/management/parquet-mapping?view=microsoft-fabric}{Kusto Parquet mappings}

\href{https://learn.microsoft.com/en-us/fabric/onelake/onelake-access-api}{OneLake ADLS-compatible access}

\href{https://arrow.apache.org/docs/r/reference/as_record_batch_reader.html}{Arrow RecordBatchReader}

\href{https://arrow.apache.org/docs/r/reference/ParquetFileWriter.html}{Arrow Parquet writer}
}
